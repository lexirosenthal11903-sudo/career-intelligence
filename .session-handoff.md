# Career Intelligence — Session Handoff (written 2026-06-23)

**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything).
**Live (updates on every push):** career-intelligence-xi.vercel.app
**Orient first:** `START-HERE.md` (where we are / what's next) → `MISSION.md` (the why). These two are the
source of truth now. Everything else is detail.

---

## ▶ DO THIS FIRST next session — verify the discovery flow for a NEW user
Lexi reported the new-user input "only asks for the CV, doesn't ask about direction/requirements." But the
**discovery conversation WAS built this session** (commit `48481bb`): after the first message the advisor
asks about direction, location, what matters — *before* analysing (`/api/intake` + `useFirstSession`).
Most likely she tested on her **existing account** (returning users skip the new-user flow) or before it
deployed. **Action:** open an **incognito window**, sign up as a brand-new user, and confirm the advisor
asks discovery questions before the reveal. If it genuinely does NOT, that's the top bug — investigate the
new-user path (signup → `/workspace?view=first`; make sure it's not falling through to the old `/input`).

## What shipped this session (all on staging, tsc + 20/20 e2e gated each push)
- **Full product audit** → `AUDIT-REPORT-2026-06-22.md` (prioritised backlog) + reusable harness
  (`tests/fixtures/personas.ts`, `tests/audit/*`).
- **Systemic fix:** `suggestedDirections` stored as a string broke recap (500) + blanked the reveal —
  normalized at the write boundary (`src/lib/profile-normalize.ts`). Recap now 200. Regression test.
- **Auth** to standard convention (existing-email signup → "log in", unknown → "sign up", calm errors).
- **Discovery conversation** — the advisor asks before it tells (`/api/intake`, `useFirstSession`).
- **Seniority done right:** roles above the user's level are filtered (jobs + scorer); the scorer's
  in-sector floor no longer overrides a deliberate low score; advisor + scorer prompts hold seniority.
- **Pathway, not a flat no:** over-reach (e.g. family office) → why-not-yet + gateway roles + the bridge.
- **Job relevance at the retrieval layer:** search real role titles as phrases + Adzuna category
  (`src/lib/adzunaCategory.ts`), killed the wild fallback.
- **Profile tab built** (was dead) — the "what I know about you" mirror. **Account:** sign out + **real
  delete-account** (with "are you sure"). Live header clock. Removed confusing "Not listed".
- **Docs decluttered:** archived 5 stale docs; added `START-HERE.md`, `MISSION.md`; mapped every
  parking-lot idea to a place in the plan. `GROUNDED-KNOWLEDGE-PLAN.md` scoped (£0).

## Pending Lexi clicks
- Run the `matched_jobs` + `recaps` migrations in Supabase if not already (recap/jobs depend on them).
  Exact SQL was given in chat; files in `supabase-migrations/`.
- Logo.dev env vars in Vercel (Lexi said done).

## Next focus (per START-HERE)
1. Finish making it **alive** — discovery's in; next is the advisor *reacting* + *feeling present*.
2. **Candidate-strength loop (the mission):** CV tailoring, cover letters, outreach, interview prep, and
   the **"why am I not hearing back?"** diagnosis.
3. Tabs still to deepen: **Direction** (make each direction clickable into detail). Documents stays empty
   until CV tailoring exists.

## Open questions waiting on Lexi
- Cookie banner: verified working (persists after Accept) — if she saw it misbehave, get specifics.
- User menu (bottom-left): currently opens Profile; she may want a J&J-style pop-up menu instead.

## Standing rules
`* staging` before touching anything · never touch `main` without explicit instruction · build
autonomously, push once, Lexi tests on the live URL · £0 (no new paid services) · `/deploy-check` before
any merge.

## Ready-to-paste prompt for the next session
> Career Intelligence — read START-HERE.md then MISSION.md, confirm `git branch` shows `* staging`.
> FIRST: in an incognito window, sign up as a new user and verify the advisor asks discovery questions
> (direction/location/requirements) before the reveal — Lexi reported it "only asks for the CV". If it
> doesn't trigger for a fresh user, fix that before anything else. Then continue the plan: finish making
> the new-user experience feel alive (advisor reacting + present), then start the candidate-strength loop
> (CV tailoring / cover letter / "why am I not hearing back?"). Model: Sonnet, normal effort unless a hard
> bug needs Opus.

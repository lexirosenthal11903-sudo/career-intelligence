# Career Intelligence — Session Handoff (updated 2026-06-29, Session 43)

## What THIS session shipped — "the rejection moment, done properly" (state model + bugs)

Cadence: grilled the STATE MODEL first → Lexi asked for research on all three forks → 3 parallel web sweeps
(`research/rejection-state-model-research.md`) → Lexi decided each fork on the evidence → built the batch →
3-agent `/code-review` (8 findings, all fixed) → verified → **pushed to staging (commit `1b40e37`)**.

1. **Real `rejected` outcome stage** — kept as a record, never auto-deleted. Advisor sets it on a definite no, with
   the caring reply and the stage change in the SAME turn. Enum synced across advisor-tools VALID, `/api/applications`
   PATCH VALID_STAGES (was out of sync — would have 400'd), and SidePanel.
2. **Quiet collapsed "Closed" area** (`CLOSED_STAGES` = rejected + archive). Soft labels "Not this time" / "Set aside"
   — never the bare word "Rejected" (fixes the archive→"Saved" mislabel). Legacy `/dashboard/applications` softened too.
3. **Transient-failure robustness** (`/api/chat` tool loop) — the rejection bug's root cause. Retries once (400ms
   backoff) on 5xx/429; only swallows a TRANSIENT failure into a warm holding line when an action already committed;
   a non-transient 4xx still surfaces (no masking real errors).
4. **Ask-why on remove + reason-routed Live-roles hide.** Removing a saved role nudges the advisor (`askAdvisor`) to
   ask once, conversationally, non-blocking. "Not for me" → new `hide_role_from_live` tool → `profiles.data.hiddenRoles[]`
   → Live-roles feed drops it (exact title+company, or title-only when company absent). "Just tidying" → nothing changes.
   Item-level ONLY. Shared `lib/role-key.ts` so server + client matching can't drift.
5. **Ride-alongs:** stale "✓ In Applications" badge re-reads on `ci:application-changed`; chat auto-scroll double-rAF;
   name-shortening tightened (never invent a nickname like "Alex").

**Verified:** tsc + lint + production build green; 19/19 unit tests; `eval:advisor` all hard rules held; one 3-agent
`/code-review` → 8 findings all fixed (incl. a feature-breaking hide-match bug when company omitted).

## ▶ LEXI'S LIVE TEST (Claude-in-Chrome, on staging — already pushed)
Use the ready-to-paste prompt at the bottom. Six steps, pass/fail each.

## ▶ NEXT SESSION — recommended focus
1. **Outcomes VOICE tuning (the deferred fine-tune beat)** — offer enthusiasm too flat; rejection warmth. Deliberate
   pass in the prompt/persona, re-run `eval:advisor`. Keep it separate from build (standing rule).
2. **Then** residual Theme B/C (filter by stage, offer/decline sections, overlay scrollbar, collapsible sidebar,
   click-a-direction, per-day feed) + standing opens (account menu, archive `/dashboard/*`, dated timeline).
3. **Deferred from this session (logged):** down-weight *similar* roles + the aggregate funnel stat → Step 3 matching
   layer (a single negative carrying that much weight is the filter-bubble harm — research-backed). Pre-existing
   em-dash UI copy sweep (several `—` in unchanged empty-state strings; none introduced this session).
4. **LinkedIn idea (logged, Step 3):** widen live-job sources beyond Adzuna+Reed via free remote feeds (Remote OK,
   We Work Remotely, Remotive, Himalayas) — research/architecture only, lower priority, does NOT change positioning.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is never QA) ·
  `/deploy-check` before any merge · receipt ("Logged ✓") the instant Lexi raises anything.
- Stage taxonomy now: saved → preparing → applied → interview → offer → **rejected** → archive. The enum lives in
  THREE places (advisor-tools VALID, `/api/applications` VALID_STAGES, SidePanel STAGE_LABELS/CLOSED_STAGES) — keep synced.
- New shared module `src/lib/role-key.ts` (role identity for hide-matching) — import it, don't re-implement.
- Run `npm run eval:advisor` when the advisor prompt/persona/voice changes. Voice rules live in several prompts that drift.

## Ready-to-paste Claude-in-Chrome live-test prompt (run on career-intelligence-xi.vercel.app)
> You are testing a live web app at https://career-intelligence-xi.vercel.app as a signed-in user with at least one
> saved role in Applications. Do each step and report PASS/FAIL with what you saw. Don't fix anything; just observe.
>
> 1. REJECTION CARE + STATE. In the chat, tell the advisor: "I didn't get the [name a saved role] role, they didn't
>    say why." PASS if: the advisor responds with genuine warmth in the SAME message (acknowledges it, normalises that
>    most people get no feedback, doesn't make you paste an email) AND there is NO cold error like "something went wrong".
> 2. CLOSED AREA. Open Applications. PASS if: that role is no longer on the active board but sits under a collapsed
>    "Closed" group, labelled "Not this time" (NOT "Rejected", NOT "Saved"). It must still exist, not vanish.
> 3. NO TAB-YANK. During step 1, PASS if the advisor did NOT move you to another tab/screen on its own.
> 4. ASK-WHY (not for me). Open a different saved role → "Remove from applications" → confirm. PASS if: the advisor
>    then asks, conversationally, why you removed it. Reply "it's not really the right field for me." Then open Live
>    roles. PASS if that role no longer appears there.
> 5. ASK-WHY (tidying). Remove another saved role the same way; when the advisor asks why, reply "just tidying up, I
>    already applied elsewhere." PASS if the advisor does NOT claim to hide it / does nothing heavy-handed.
> 6. AUTO-SCROLL + NAME. Send a long multi-paragraph message. PASS if the view scrolls to show the newest reply in
>    full. Also PASS if across the whole test the advisor never invents a shortened nickname for your name.
> Report each step PASS/FAIL with one line of evidence.

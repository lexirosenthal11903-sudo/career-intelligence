# Career Intelligence — Session Handoff (written 2026-06-26)

**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything).
**Live (updates on every push):** career-intelligence-xi.vercel.app
**Orient first:** `START-HERE.md` → `MISSION.md` → `REBUILD.md` → this file.

---

## ▶ What this session did (2026-06-26)

**1. "Why am I not hearing back?" diagnosis — SHIPPED, corrected, verified, live.** Step 2's anchor feature.
Built as advisor **knowledge + behaviour, NOT a tool** (the value is a diagnostic conversation, not an
artifact) — Lexi chose conversational-only. Lives in `ADVISOR_PERSONA.md` ("Diagnosing 'why am I not hearing
back?'" section, source of truth) + a tight module in `src/app/api/chat/route.ts` system prompt.
- Behaviour: lead with the 140:1 base-rate reframe → ask 2-3 facts (volume/period/targeting/level/sector) →
  **explore probable causes WITH the user, never assert one as fact** → end in an existing tool. Two-tier
  honesty enforced in-prompt (🟢 carried with authority; 🔴 banned: "ATS rejected you", "7 seconds", "3 in 5").
- **Lexi's correction (important, now locked):** the advisor must NEVER name one cause as *the* reason a
  given employer went silent — we can't know. Even when the user reveals a real mistake, frame it as "what
  stacks the odds / what to change first", not the proven reason. Critique the approach, never the specific "no".
- Verified against the real API (2 runs, throwaway script — see "next session" #1). Commits: `1ae9d3c`,
  correction `3e0add0`.

**2. CLAUDE.md pruned 389 → 200 lines.** It was bloated AND stale (pointed at archived ROADMAP/PLAYBOOK,
claimed "Phase 0 in progress"). History moved to `archive/CLAUDE-historical-detail-2026-06-26.md` (not
deleted). Now active-rules-only, pointers fixed. Commit `7f10d25`.

**3. Adopted ALL 11 Claude Code working practices** (from the Anthropic guide PDF on Lexi's Desktop). Full
reference: **`WORKING-PRACTICES.md`** (new). Two behavioural rules baked into CLAUDE.md:
- **Rule 11 — Claude owns the timing.** Lexi won't know when to use a practice (subagents, writer/reviewer
  split, `/clear`, `/grill-me`, push-it) — **Claude proactively calls the moment, every time.**
- **Rule 10 — auto-prune.** Prune CLAUDE.md in the same edit when adding; never make Lexi remind.
- **Rule 5 — close the loop.** After any *code* slice, run `/code-review` + `/simplify` before Lexi tests.
- Subagents: Lexi upgraded her plan for headroom and wants to use them properly — how-to is in WORKING-PRACTICES.md.
- Commit `028d1b8`. Memory: `feedback_claude_code_practices.md`.

## Where we are
Step 2 (candidate-strength loop) — the heart. **Done + persisting:** CV tailoring, cover letters,
Applications folder, the "why am I not hearing back?" diagnosis. **Next features:** outreach/warm intros
(needs a `/grill-me` first — open design Qs: no referral %, the relationship-building angle), then interview prep.

## ▶ NEXT SESSION — build the test/eval infrastructure (fresh, focused)
The guide's #1 principle is "give Claude a runnable pass/fail." We don't have one — today's diagnosis was
hand-tested with a throwaway script. Build it, in this order (dependency: lint-to-green before CI):
1. **Advisor-behaviour eval** — convert today's throwaway script into a kept, rerunnable eval (rerun whenever
   the advisor system prompt changes). The script logic is in this session's history; it extracts
   `ARLO_SYSTEM_PROMPT` from `chat/route.ts`, attaches a synthetic user context, hits the API, and asserts the
   diagnosis rules (reframe first, no banned myths, no false-confident single cause). Reads `ANTHROPIC_API_KEY`
   from `.env.local`.
2. **Thin Playwright smoke test** of the core flow: input → loading → onboarding bridge → dashboard → roles.
   Smoke, not exhaustive — flows still change in Step 2.
3. **Clear the lint backlog to green** — `npm run lint` = 38 errors + 9 warnings, ALL pre-existing in untouched
   files (20× `next/no-html-link-for-pages`, 15× `react-hooks/set-state-in-effect`, 8 unused vars, misc). The
   link errors are minor real UX bugs; the set-state ones need judgment (some warrant a justified disable).
4. **CI (GitHub Actions)** — run lint + tests on push. Only after lint is green.

## Other open threads (not next, but tracked)
- **Process session done** (this was it) — practices adopted. Parking-lot row for it can be marked done.
- **AUDIT-REPORT-2026-06-22.md** backlog — saved-roles view, clickable directions, logos — chip between features.
- **Pre-launch non-negotiables** (Lexi to handle): REED_API_KEY in Vercel, ICO registration, GitHub token
  rotation, privacy policy + terms, user deletion, Sentry/Analytics. Now also: CI + lint-green (CLAUDE.md "Current Phase").

## Standing rules
`* staging` before touching anything · never touch `main` without explicit instruction · build autonomously,
push once, Lexi tests on the live URL at flow milestones · £0 (no new paid services) · `/deploy-check` before
any merge · `/session-handoff` at the END of every session · **Claude owns the timing of every working practice
(CLAUDE.md rule 11) — prompt Lexi proactively.**

## Suggested skills for next session
- **`webapp-testing`** — Playwright smoke test of the core flow (#2). Primary skill for the session.
- **`tdd`** (reinstall from `~/Desktop/Claude Code/All Installed Skills/`) — write the eval/tests properly.
- **`systematic-debugging`** — for the lint set-state-in-effect fixes that need judgment (#3).
- **`verification-before-completion`** — prove the eval + CI actually run green before claiming done.
- **`deploy-check`** — only if a merge to main comes up (it shouldn't this session).

## Ready-to-paste prompt for next session
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> Fresh focused engineering session: build the **test/eval infrastructure** (our "runnable pass/fail" gap).
> Order: (1) turn the throwaway advisor-behaviour test into a kept rerunnable eval; (2) thin Playwright smoke
> test of the core flow; (3) clear the lint backlog to green; (4) wire CI (GitHub Actions) on push — lint must
> be green before CI. Use `/code-review` + `/simplify` after each code slice. Switch to **Opus** for this
> (CI/test architecture) — `/model claude-opus-4-8`. Challenge the approach before building.

# Career Intelligence — Session Handoff (updated 2026-07-01, Session 48)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`).** The API is real money, NOT the Max
   plan. Run the FULL `eval:advisor` (~$0.20) ONLY on a material advisor *chat-prompt* change — never twice a
   session, never "to be safe". Use `npm run eval:advisor:quick` (~$0.03) for spot-checks; lean on the £0 unit/e2e.
3. If the advisor 400s "credit balance too low", it's the balance, not a bug.

## ▶ WHAT SESSION 48 DID — Roles-vs-Applications IA DECIDED (design session, Fable 5, NO code)
The S47 QA finding ("Roles for you" detail vs "Applications" detail bleeding together) went through a full
evidence pass — prior-art research (Teal/Huntr/Simplify/Otta/LinkedIn/NN&g) + a codebase IA map — and Lexi
locked both forks. **The build contract is `SPEC.md` (repo root). Read it before the build — it has the model,
the technical shape, and objective done criteria.** Commit `3a0143f`, pushed to staging.

**The locked model — "one record, two lenses":**
- One record per role (`saved_applications` = source of truth). Live-role detail = evaluation lens ("should I
  go for this?" — company, fit, description, reach-out DOOR). Application detail = management lens ("where's my
  application at?" — stage, CV, cover letter, outreach THREAD + chips, notes).
- **Prep auto-saves, never auto-advances:** draft_outreach / tailor_cv quietly create the application at stage
  "Saved" if absent; NEVER move an existing stage (industry invariant — only real apply events advance).
- Status chips leave the live-role view entirely; after save the live view shows one quiet "In your
  applications" handoff link. No management info in two places.
- Rejected: hard promotion, Teal-style merged mega-card, no-promotion templates.
- Superseded role-interest spec → `archive/SPEC-role-interest-mentoring-2026-06-27.md` (that build shipped).
- Known gap logged, out of scope: live-role view thin on real COMPANY evidence → grounded knowledge (Step 3).

## ▶ NEXT (in order — Lexi picks 1 or 2)
1. **IA build against `SPEC.md`** (Opus). Key build points: move `OutreachSection` from `RoleDetail` into
   `SavedJobDetail`; prep actions upsert `saved_applications` (stage `saved`, mirror the "Interested"
   dual-write) + emit `application-changed`; handoff link via existing `initialJobId` path. ⚠️ Key-alignment
   risk: `outreach` is keyed by role_key (title+company), applications by `job_id` — likely add `job_id` to
   outreach. Advisor-prompt change is contextual only → quick subset check, NOT the full eval.
2. **Interview prep** (last major candidate-loop piece; `research/interview-prep-research.md` exists — check
   whether it meets the 7-point research bar before building). Opus.
3. **#6 expired listings** — last HIGH audit item; needs new data + a UX decision. Waits until nearer launch.
4. Smaller logged: #10 (edit/delete advisor memory notes), account menu, archive dead `/dashboard/*`.

## Model guidance for next session
- **Both build candidates → Opus** (Sonnet for mechanical parts). Fable was for the S48 IA decision only —
  do NOT stay on Fable for builds (double Opus cost).

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is never
  QA) · `/deploy-check` before any merge · design sessions and build sessions are separate.
- Pre-commit hook runs the Playwright e2e suite and blocks on failure (`/tmp/meridian-e2e.log`). Don't bypass —
  re-run to classify; a real failure repeats (S46 proved a "flake" can be a real bug).
- Pure helpers needing unit tests go in dependency-clean leaf modules (no `@/` imports): `outreach.ts`,
  `chat-reply.ts`, `api-response.ts`, `merge-list.ts`, `sanitize.ts`, etc. `node --test` can't resolve `@/`.
- Voice rules live in several prompts (chat, recap, tailor_cv, cover-letter) — keep in sync; output is sanitised
  by `stripDashes`. Em dashes are banned in user-facing copy AND context-helper strings (match `user-state.ts`).

## ▶ READY-TO-PASTE NEXT-SESSION PROMPT
> Career Intelligence — read `.session-handoff.md`, `START-HERE.md`, and **`SPEC.md`** (the agreed
> Roles-vs-Applications IA contract); confirm `git branch` shows `* staging`. Session 48 DECIDED the IA
> ("one record, two lenses" — prep auto-saves at stage Saved, never auto-advances; all tracking moves to the
> application detail; live view = evaluate + reach-out door + handoff link). Nothing is built yet. Today is
> the **IA BUILD against SPEC.md** — plan mode first, verify against SPEC's done criteria, watch the
> outreach role_key vs job_id join risk, quick subset check only for the advisor-context change (NOT the full
> eval). **Opus** (`/model claude-opus-4-8`). (Alternative if I'd rather: interview prep — check
> `research/interview-prep-research.md` against the research bar first. Also Opus.)

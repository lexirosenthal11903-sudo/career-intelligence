# Career Intelligence — Session Handoff (updated 2026-06-27 PM #2)

## 🔔 REMINDER FOR LEXI (surface this first, every next session until she confirms)
**Lexi still needs to live-test on career-intelligence-xi.vercel.app — she chose to do it AFTER the
interview-prep research is done.** Two things to test:
1. **Outreach Slice 2** — (a) the three entry points: Roles → Role types tab shows a "not all roles are
   advertised" nudge under the cards; click a direction → "Help me reach out to someone in X"; Roles →
   Live listings with no jobs → "Ask who to reach out to". (b) Paste-to-tailor: in any advisor chat say
   "I found this person" + paste a LinkedIn post/bio → the draft should reference it specifically.
2. **The "Arlo" rename** — the advisor name is gone everywhere user-facing; it now reads "Career
   Intelligence" / speaks first person. Check nothing looks odd (panel headers, landing page, chat).

## Latest session (2026-06-27 PM #2) — what got done
1. **Outreach Slice 2 DONE + pushed (`f5b8eda`).** Three advisor-led entry points routing a contextual
   prompt into the existing `draft_outreach` (no backend): few/no live listings, a specific direction
   (role detail), and a gentle role-types nudge. Plus **paste-to-tailor**: `draft_outreach` gained an
   optional `foundContext` param — user pastes a post/profile THEY found, message references it. Processed
   transiently, capped 1500 chars, never stored as a contact. (GDPR: pasted text lands in the user's own
   chat history → flagged for the pending solicitor review.)
2. **"Arlo" dropped from ALL user-facing copy + pushed (`ee3af50`).** Executed the decided move to no
   character name (CLAUDE.md). 13 files: panel headers → "Career Intelligence"; chat first person ("What
   I see in you", "Tell me →"); "Arlo is thinking…" → "Thinking…"; landing prose; recap system prompt.
   `ADVISOR_PERSONA.md` name section marked dropped (supersedes the 2026-06-10 "Arlo confirmed"). Internal
   identifiers (CSS classes, `arlo-visible` key, `useArloChat`, test titles) intentionally left — not
   user-facing. Memory `project_advisor_name_dropped` written so it's never reintroduced.
3. **Roadmap updated + pushed (`b8a90a5`).** Slice 1+2 marked done. Logged 3 new features: **voice input**
   (mic → transcribe, Web Speech API = £0, near-term), **voice call** (later, breaks £0), and **live mock
   interview** (the advisor runs a mock calibrated to interview type — strongest for the mission).
4. **Verified green** at every step: tsc + lint + build + 19 unit + 23 e2e. Landing page screenshotted.

## ▶ NEXT SESSION — the focus (fresh session, Opus, Lexi's call)
**Interview-prep grounding research.** Per the research-grounded-building standard (no grounding, no build):
write a credible, cited UK-2026 research report before building any interview-prep code. Scope it to cover
BOTH parts of the feature: (1) **planning** — likely question types by role/sector + how to prepare; and
(2) the **live mock interview** — the real interview FORMATS (competency/behavioural, technical, case,
strengths, panel, screening call) and what actually works in each. Follow the 8-point bar + independent
reviewer audit + known-gaps section. Output → `research/interview-prep-research.md`. THEN build.

After the research + build: Lexi live-tests Slice 2 + the rename (see reminder above).

## Operational / standing
- Email still in Resend **TEST MODE** (`onboarding@resend.dev`) — codes only reach Lexi's own inbox; verify
  a real Resend domain pre-launch.
- Anthropic API credits power production — if the live advisor errors, check Billing first.
- `npm run eval:advisor` — hold until the prompt settles (interview prep will touch it again). Run once
  before any merge to main, not mid-build.
- Standing rules: `* staging` before touching anything · never touch `main` without explicit instruction ·
  build autonomously, push at milestones · £0 for NEW platforms · `/deploy-check` before any merge · no em
  dashes in user-facing copy · advisor speaks as "Career Intelligence", first person, no character name.

## Ready-to-paste prompt for next session
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> First, SURFACE THE REMINDER at the top of the handoff (I still need to live-test Outreach Slice 2 + the
> Arlo→Career Intelligence rename — but AFTER this session's research). Today's focus: the interview-prep
> grounding research → `research/interview-prep-research.md`, covering both planning (question types by
> role/sector) and the live mock interview (the real interview formats + what works in each). Follow the
> research-grounded standard: 8-point bar, independent reviewer audit, known-gaps section, all-ages scope,
> review date. No build until the research passes. Opus, normal effort.

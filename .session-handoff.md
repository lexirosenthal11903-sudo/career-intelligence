# Career Intelligence — Session Handoff (written 2026-06-26)

**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything).
**Live (updates on every push):** career-intelligence-xi.vercel.app
**Orient first:** `START-HERE.md` → `MISSION.md` → this file.

---

## ▶ Why this handoff exists
A research session (2026-06-25) was wiped before commit/handoff. The work survived as files on disk but
was floating untracked. Recovered + committed 2026-06-26 (commit `8211873`). No code was lost — the tree
was already clean; only the research doc, the documents migration, and a plan file were uncommitted.
**Lesson: run `/session-handoff` at the end of every session — this is why.**

## Where we actually are (verified against git, not memory)
- **Step 2 — the candidate-strength loop — is in progress.** Shipped and committed:
  - CV tailoring (Step 2 slice 1) — `tailor_cv` advisor tool + `TailorCVModal` + `/api/tailor-cv`
  - Cover letter generation (Step 2 slice 2)
  - Applications folder — saved-roles hub with per-role documents
  - Batch of UX fixes from Lexi's last test (tabs, CV documents panel, OAuth reset, intake questions)
- **Persistence:** `documents` table migration (`supabase-migrations/20260624_documents.sql`) **was run in
  Supabase — confirmed by Lexi 2026-06-26.** CV tailoring + cover letters persist correctly.
- **Research gate done:** `research/application-effectiveness.md` (417 lines) — UK early-career, 9 threads,
  two-tier 🟢 evidence / 🟡 convention / 🔴 myth structure. Headline: **140 applications per UK grad
  vacancy (highest in 3 decades)** — a strong app hearing nothing is the statistical norm, not a fault.
  This is the evidence base the remaining Step 2 prompts must be written against.

## Next focus (per START-HERE Step 2)
The research session was the gate before writing these. Build order:
1. **"Why am I not hearing back?" diagnosis** — the anchor feature. Lead with the 140:1 reframe, then
   move to specific probable causes. Must respect the two-tier rule: never dress 🟡 convention as 🟢
   evidence; never use anything 🔴.
2. **Outreach / warm intros**, then **interview prep**.
3. Alongside: chip at the AUDIT-REPORT backlog (saved-roles view, clickable directions, logos).

## Standing rules
`* staging` before touching anything · never touch `main` without explicit instruction · build
autonomously, push once, Lexi tests on the live URL at flow milestones · £0 (no new paid services) ·
`/deploy-check` before any merge · `/session-handoff` at the END of every session (non-negotiable now).

## Ready-to-paste prompt for next session
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> We're in Step 2 (candidate-strength loop). CV tailoring + cover letters are done and persisting. Next is
> the **"why am I not hearing back?" diagnosis** feature, grounded in research/application-effectiveness.md
> (lead with the 140:1 base-rate reframe; respect the 🟢/🟡/🔴 evidence tiers). Challenge the approach
> before building.

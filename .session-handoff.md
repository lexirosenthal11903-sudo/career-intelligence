# Career Intelligence — Session Handoff (updated 2026-06-29, Session 44 + 44b)

## ▶ NEXT SESSION = the STATE-SYNC consolidation (Opus, normal→high effort)
Read **`STATE-SYNC-AUDIT.md` first** — it's the whole plan. Lexi was (rightly) frustrated that the advisor
kept saying things that didn't match the screen and that I patched symptoms instead of the architecture. The
audit maps ALL 14 divergences (it found 6 she hadn't even caught) and the root cause: four role stores keyed
differently + synthetic `chat-<slug>` ids vs real listing ids, with no single source of truth. The fix is
ONE `assembleUserState()` both advisor + UI derive from, `roleKey` as the join key, deterministic backstops
(name + time, like `stripDashes`), and a **board-truth drift test** so CI catches this, not Lexi. The name
bug + time bug are just two instances of this class, so do the consolidation, don't fix them piecemeal.
Fix-first order (from the audit): #1 synthetic-id badge, #2 rejected-direction still shown, #3 preferredName
not shown in UI, then #4 board-hidden-when-saved_jobs-empty, #5 salary invisible, #11 time, then #6 expired
listings (needs new data: an Adzuna/Reed liveness check). This is architectural + multi-file → **Opus**.

S44/44b shipped the board-truth cluster + rejection-path stability + the time/closed-role read fixes (all
green, pushed `3cb3a26`, `27664ff`). Those were the right fixes but symptom-level; the consolidation is the
structural close-out.

- **Name shortened to "Alex" then denied (do this first).** The recap card (a separate surface) called
  Alexandra "Alex"; the chat advisor then truthfully said it never used "Alex" (the card did), which reads
  as gaslighting. Two things to settle: (1) where "Alex" comes from, likely a stale stored `preferredName`
  on her account, which needs a read of her real data (the prod-data check was correctly BLOCKED this
  session, so Lexi must run a one-line check or grant the read); (2) a deterministic name guard so NO
  surface (recap, chat) can emit a shortened form unless she has set a preferredName, plus cross-surface
  awareness so the chat advisor doesn't flatly deny what another surface said.
- **Then the smaller logged items:** scroll-up to read earlier conversation; recap "Earlier / Today"
  framing is confusing; the advisor forgot prior info (review `remember` reliability); career-coaching
  research (we did mentorship, not coaching); and the **sycophancy guardrail** (Lexi's own sharp point:
  the line between pleasing the user and genuinely building momentum, belongs in the deferred voice pass).
- **Known minor (S44 review, low-risk, logged):** centralise the duplicated closed-stage set; `saved_jobs`
  sync is a non-atomic read-modify-write (same-user two-surface race could clobber a field).

## What Session 44 shipped — board-truth cluster + rejection-path stability (pushed `3cb3a26`)

Cadence: reproduction-FIRST debugging (a harness drove the real tool loop with logging; the canonical
rejection path was already good post-S43, so no blind voice edits) → fixed the genuinely reproducible
bugs → Lexi live-tested mid-session and surfaced the board-drift cluster → chose scope (board-truth +
autoscroll) → built → two `/code-review` passes (findings addressed) → verified → pushed.

1. **Wrong-role stage match** — `set_application_stage` scored matches by exactness and now ASKS on genuine
   ambiguity (shared `roleKey` dedup) instead of grabbing the first substring hit. Prompt nudge added too.
2. **Error + green-log race** — a committed action is never stranded behind a cold error (warm holding line,
   logged for Sentry, transient or not); a non-JSON upstream body (502 HTML) no longer crashes the turn.
3. **Closed role drops out of Live roles + loses the badge** — `set_application_stage` syncs
   `saved_jobs.job_data.status` (closed → 'passed', live → 'interested'). Fixes the Trainee Business Analyst
   case (showed "In Applications" in Live roles while Closed in Applications).
4. **The board is the only source of truth for outcomes** — chat prompt rule ("never assert an offer/
   interview from memory/threads, reconcile by asking") + the recap card is now board-aware (reads
   `saved_applications`, excludes closed stages, won't re-open a rejected role) + recap cache busted on any
   stage change. Fixes the confabulated "Law Society offer".
5. **Autoscroll on send** — drives the scroll container to `scrollHeight` so the newest message lands ABOVE
   the fixed composer (it was being parked behind it, which read as "no scroll").
6. **eval** — tightened the spray-and-pray regex (it false-positived on a correct anti-spray reply,
   "generic volume is nearly invisible"); the advisor's behaviour was right.

**Verified:** tsc + lint + production build green; 19/19 unit tests; `eval:advisor` all hard rules held (one
false positive fixed); two 3-/single-agent `/code-review` passes, findings addressed or logged.

## ▶ LEXI'S LIVE TEST (Claude-in-Chrome, on staging — already pushed `3cb3a26`)
Use the ready-to-paste prompt at the bottom. Tests the three things she reported.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is
  never QA) · `/deploy-check` before any merge · receipt ("Logged ✓") the instant Lexi raises anything.
- Reproduction-first debugging works well: a small harness driving the real tool loop (mock tool results,
  loop like `/api/chat`) catches sequencing/leak/match bugs cheaply. Node runs `.ts` via a tiny `@/` alias
  loader. Don't blind-edit working voice code against a bug you can't reproduce.
- Voice rules live in SEVERAL prompts (chat, recap, tailor_cv, cover-letter) — keep them in sync; the recap
  is its own surface (`/api/recap`) with its own prompt + `stripDashes`.
- Run `npm run eval:advisor` when the chat prompt/persona changes (the recap isn't covered by the eval).
- Reading live prod user data is BLOCKED by the safety classifier unless Lexi names the prod DB as the
  target / grants it. For the name bug, she runs the check or grants access.

## Ready-to-paste Claude-in-Chrome live-test prompt (run on career-intelligence-xi.vercel.app)
> You are testing a live web app at https://career-intelligence-xi.vercel.app as a signed-in user with at
> least one saved role in Applications. Do each step and report PASS/FAIL with what you saw. Don't fix
> anything; just observe.
>
> 1. CLOSED ROLE LEAVES LIVE ROLES. In chat, tell the advisor you didn't get one of your saved roles (name
>    it). Then open Live roles. PASS if: that role is NO LONGER listed there, and does NOT show an "In
>    Applications" badge anywhere. Then open Applications. PASS if it sits under the collapsed "Closed" group.
> 2. NO PHANTOM OUTCOMES. Ask the advisor: "where do my applications actually stand right now?" PASS if it
>    only mentions outcomes (offer/interview/applied) that genuinely appear on your Applications board, and
>    does NOT claim an offer or interview that isn't there.
> 3. AUTOSCROLL ON SEND. Send a few messages, including one long multi-paragraph one. PASS if after EACH
>    send the view scrolls so your message and the reply are fully visible above the message box, with no
>    manual scrolling needed.
> 4. WRONG-ROLE SAFETY (if you have two similarly-named roles). Refer to one vaguely ("I didn't get the
>    analyst one"). PASS if the advisor ASKS which one you mean rather than silently changing the wrong role.
> Report each step PASS/FAIL with one line of evidence.

## Still OPEN (logged in FEATURE-ROADMAP.md "Session 44 live-test findings")
Name bug (next focus) · scroll-up history · recap Earlier/Today framing · memory-forgetting · career-coaching
research · sycophancy guardrail (voice pass) · the two low-risk S44-review minors.

# Career Intelligence — Session Handoff (updated 2026-06-27 PM #5)

## ▶ NEXT SESSION — build the role-interest mentoring redesign (the heart)
**Read first:** `SPEC.md` (the contract — agreed this session) → `START-HERE.md` → this file. Confirm
`git branch` shows `* staging`. **Model: Opus** (advisor-prompt reasoning + memory architecture).

The spec is fully agreed and self-contained — start in plan mode, plan the build against `SPEC.md`'s 8 done
criteria, then build. Headlines (full detail in `SPEC.md`, do not duplicate here):
- Clicking "interested" saves the role + the advisor opens a CURIOUS conversation (one real question first,
  then adapts to the clarity dial). Honest on weak fit. Docs offered only at the right moment. Never
  fabricates company/process facts (signposts a real source). Remembers the "why" to sharpen future recs —
  but NOT like-for-like (informs values/direction, never narrows the funnel).
- Scope = **conversation + memory only**; the visible "plan" screen is deferred.
- **Touches the advisor prompt → `npm run eval:advisor` (17 personas) MUST pass before any merge.** Add a
  persona exercising interested → mentoring → honest-fit.
- Likely files: `advisor-prompt.ts` (behaviour), `SidePanel.handleInterested` (opener trigger),
  `saved_jobs.job_data` + `buildUserContext` (the "why" memory, as a values signal not a similarity filter).

## What THIS session shipped
1. **Foundation pass — DONE + pushed to staging** (commit `5afe50f`). Rate limits on `/api/extract` +
   `/api/auth/check-email`; standard-UX fixes (auto-scroll, input height, composer "+" CV re-upload, lead
   badge off amber, hint not-a-button, LinkedIn link suppressed when a person is pasted, account overflow,
   save-job validation, `.doc` removed from pickers); advisor now holds saved-role detail so it stops
   re-asking for a JD. Self-verified: tsc/lint clean, build green, first-session render smoke-tested.
   ⚠️ Honest caveat: rate limiters fail open locally (no Upstash) — verified by code-path parity, not a live
   load test. Lexi did NOT retest (by agreement).
2. **Prior-art map — DONE → `PRIOR-ART-MAP.md`.** Four-questions across 13 built + 4 planned features.
   5 changes logged to `FEATURE-ROADMAP.md` (account popover pattern, `/dashboard` archive, time-aware +
   emotional memory, recap re-entry format). Read-with-Lexi done (distilled, not full read).
3. **`SPEC.md`** for the redesign (above). The "standards bar" Lexi remembered is captured INSIDE the spec
   ("the bar that makes it ours"), per WORKING-PRACTICES Bonus 1 — no standalone STANDARDS.md.

## Flagged finding (decide together — nothing touched)
`/dashboard/*` is a DEAD legacy surface (live app is entirely `/workspace`; dashboard only links to itself;
skills now live inside the role detail). Archive in a walk-through-together pass, ideally after the redesign.
Two Tier-3 audit fixes that lived only there were deliberately skipped. ⚠️ Nothing wiped until reviewed
page-by-page. Logged in `FEATURE-ROADMAP.md` + `PRIOR-ART-MAP.md` Part 3.

## Also worth a copy/voice pass (fine-tuning, separate stream)
The locked first-session opener still contains em dashes ("what you actually want — and then…",
"No forms, no quiz — just tell me"). Flagged, not fixed (voice ≠ this build). Breaks `feedback_no_em_dashes`.

## Operational / standing
- `* staging` before touching anything · never touch `main` without explicit instruction · build
  autonomously + self-verify (no Lexi retest) · push at milestones · `/deploy-check` before any merge ·
  no em dashes anywhere · advisor speaks as "Career Intelligence", first person, no name.
- Rate limiting now FULLY covers all public endpoints (memory `project_rate_limiting_deferred` updated).
- Email still in Resend TEST MODE (codes only reach Lexi's inbox) — verify a real domain pre-launch.
- Pre-launch non-negotiables unchanged (CLAUDE.md): REED_API_KEY in Vercel, ICO registration, revoke the OLD
  GitHub token, privacy/terms, Sentry + Analytics, solicitor review of outreach data handling.

## Suggested skills for next session
- `/grill-me` already done (this session) — go straight to **plan mode** then build.
- `systematic-debugging` + `zoom-out` — reinstall at the start of the build session (engineering work).
- `/code-review` + `/simplify` after the code slice; `webapp-testing` (Playwright) for the flow.
- `/deploy-check` + `npm run eval:advisor` before any merge discussion.

## Ready-to-paste prompt for next session (fresh chat)
> Career Intelligence — read SPEC.md then START-HERE.md + .session-handoff.md, confirm `git branch` shows
> `* staging`. Build the role-interest mentoring redesign from SPEC.md (the heart of Step 2): clicking
> interested saves the role + the advisor opens a curious, adaptive mentoring conversation — honest on fit,
> docs as steps, grounded facts only (signpost when unknown), remembers the "why" to sharpen future recs
> without narrowing to like-for-like. Conversation + memory only; no visible plan screen. Start in plan mode,
> plan against SPEC.md's 8 done-criteria. Touches the advisor prompt → run `npm run eval:advisor` before any
> merge. Opus.

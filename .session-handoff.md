# Career Intelligence — Session Handoff (updated 2026-06-27 PM #4)

## 🔔 START THE NEXT WORK IN A FRESH CHAT (Lexi's call)
This session diverted from the planned bug pass into a much bigger, more important conversation about HOW we
build. That was the right call. Everything is captured below + in the docs so nothing is lost. The actual
build/audit work starts fresh next chat.

## What this session decided (the important part)
The session started as the Outreach Slice 2 bug-fix pass but became a strategy conversation. Key outcomes:

1. **Foundation audit done → `AUDIT-REPORT-2026-06-27.md`.** Verdict: the foundation is SOUND, not rotten
   (auth on every user-data route, GDPR deletion correct, rate-limit framework live — Upstash keys CONFIRMED
   set in Vercel Prod+Preview). Gaps: 2 public endpoints lack rate limits (`/api/extract`,
   `/api/auth/check-email`), the standard-UX bugs Lexi hit, a few minor inconsistencies, and the account
   control (bottom-left) is sub-standard (text overflow + jumps to Profile instead of a popover menu).
2. **The big reframe — role-interest = a MENTORING conversation, not document output.** Clicking a role /
   "I'm interested" should NOT jump to CV + cover letter. It should start an adaptive conversation: why
   interested → honest alignment → gaps → company → process → a plan. Documents are steps inside that plan.
   The captured "why" feeds future recommendations (the compounding moat). Paced, never an interrogation.
   Full item in `FEATURE-ROADMAP.md` Step 2 (high priority). Design + grill BEFORE building.
3. **New permanent working practice — the four questions before EVERY build** (`WORKING-PRACTICES.md` §9):
   (1) solved already? (2) who does it best + what to copy? (3) how does ours compare? (4) keep/change/layer?
   Two principles: **copying is the start line not the finish** (still measured vs our research/mission +
   advisor layer on top), and **understand before changing — never wipe-and-replace blind**.
4. **Lexi: no more retesting from her.** Claude must self-verify and identify issues itself; Lexi is never
   the QA. (Reinforces close-the-loop + "Lexi is never first to spot obvious bugs".)

## ▶ NEXT SESSION — the focus (fresh chat, Opus, START IN PLAN MODE)
**Two deliverables, run together (one is keyboard work, one is research/judgment):**

### A. The prior-art map (the priority deliverable — Lexi's main ask)
A reference DOC (changes zero code) that runs the four questions across EVERY feature: onboarding, the advisor
itself, CV building, CV tailoring, cover letters, roles matching, applications tracking, outreach, interview
prep, account menu, documents, profile, recap. For each: solved elsewhere? best-in-class reference(s)? how
ours compares? keep/change/layer-on-top plan (baseline → our research/mission/advisor layer). 
- **Sequence:** everything ALREADY BUILT first (the priority — clean up what exists), THEN everything planned.
  Quick, easy future wins can jump the queue if they speed things up.
- Known references to start from (verify + expand): account menu → Linear/Notion/Slack/ChatGPT popover; CV
  building → Reactive Resume (open-source, study it), Rezi/Teal (ATS); roles matching → Otta (Welcome to the
  Jungle), LinkedIn, Teal. Use WebSearch for current best-in-class. Suggest a subagent for the research fan-out.
- Output: a new doc (e.g. `PRIOR-ART-MAP.md`). Bring it to Lexi to read together BEFORE anything is built.
- ⚠️ Nothing gets wiped/replaced off the back of the map without Lexi understanding + agreeing, feature by feature.

### B. The foundation pass (the safe bug fixes — pattern-agnostic, do alongside A)
One batch, push once, NO retest from Lexi (Claude self-verifies via `/code-review` + `/simplify` + lint/build).
All detail in `AUDIT-REPORT-2026-06-27.md`. In short:
- Tier 1: add IP rate limit to `/api/extract` + `/api/auth/check-email` (mirror `checkIntakeRateLimit`).
- Tier 2: chat auto-scroll (`useArloChat.ts:254` nearest→end); textarea height reset on send
  (`ChatPane.tsx:763`); wire the "+" CV re-upload (`/api/extract` + `saveCvToProfile`); role-interest passes
  role details so the advisor stops re-asking (tactical patch now; real fix = memory layer with the redesign);
  make "I'm interested → Applications" obvious + the follow-up feel like the advisor reacting; amber "1" →
  neutral (`workspace.module.css:414`); "Don't scroll endlessly" hint non-interactive (`SidePanel.tsx:275`);
  LinkedIn link reframe when a person is pasted (`advisor-tools.ts:606`); account control overflow + popover.
- Tier 3: `.doc` accept vs extract mismatch; SkillsPage dead upload input; save-job POST validation.
- Note: the 3 Outreach Slice 2 entry points still need porting to the live `/workspace` SidePanel (roles-list
  nudge, no-listings prompt, per-direction reach-out) — the role-detail "Reaching out"/"Outreach draft"
  entry point already exists on the workspace surface. This rolls into the foundation pass.

### THEN: the role-interest mentoring redesign (the heart) — design + grill first, build after.
The advisor memory layer lands here (so it stops re-asking + carries what the user has done). Touches the
advisor prompt → run `npm run eval:advisor` after it settles, before any merge.

## Captured to FEATURE-ROADMAP this session
Role-interest mentoring flow (high priority) · advisor carries memory of what the user has done · outreach
saved per role + a cross-role "people I've reached out to" log · advisor can open a tab on request.

## Operational / standing
- Upstash rate-limit keys CONFIRMED in Vercel (Prod+Preview). Memory `project_rate_limiting_deferred` is STALE.
- Email still in Resend TEST MODE — codes only reach Lexi's inbox; verify a real domain pre-launch.
- `npm run eval:advisor` — run once before any merge to main, not mid-build.
- Standing: `* staging` before touching anything · never touch `main` without explicit instruction · build
  autonomously + self-verify (no Lexi retest) · push at milestones · £0 for NEW platforms · `/deploy-check`
  before any merge · no em dashes anywhere · advisor speaks as "Career Intelligence", first person, no name.

## Ready-to-paste prompt for next session (fresh chat)
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> Today, two things run together: (A) build the PRIOR-ART MAP — a reference doc running the four questions
> (WORKING-PRACTICES §9) across every feature we've built, then every feature planned; everything already
> built first (priority), quick future wins can jump the queue; copying is the start line not the finish, and
> nothing gets wiped without me understanding it first; bring it to me to read together before building. (B)
> The FOUNDATION PASS — the safe bug fixes in AUDIT-REPORT-2026-06-27.md, one batch, you self-verify
> (/code-review + /simplify + lint/build), I do NOT retest. Then we design the role-interest mentoring
> redesign (the heart) — grill me first. Opus, plan mode. Suggest a subagent for the prior-art research.

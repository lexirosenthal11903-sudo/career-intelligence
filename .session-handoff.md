# Career Intelligence — Session Handoff (updated 2026-06-29, Session 42)

## What THIS session shipped (Theme A — "the advisor drives state + holds you through it")

Cadence as agreed: grilled once upfront → Lexi asked to research-ground all three decisions first → web research
sweep (cited in `research/rejection-care-and-navigation-research.md`) → built the batch → 3-agent `/code-review`
→ fixes applied. **Not yet committed/pushed — awaiting Lexi's go to push to staging for live test.**

1. **A1 — advisor drives Applications from conversation, live.** Verified the write already worked; the real gap was
   UI reflection + nav. `save_job` + `set_application_stage` now emit `application-changed`; the Applications list,
   saved-role detail, and left-nav count/Recent re-read live. `save_job` now writes `status:'interested'` so
   chat-saved roles count in the nav (a pre-existing miss the review surfaced).
2. **Navigation bridge (Lexi's refinement): user-requested ONLY.** New `open_surface` tool — the advisor opens a
   surface only when explicitly asked; a state change never yanks them to a tab. Shared `lib/surfaces.ts` (no drift).
3. **A2 — outcome acknowledgement:** offer = genuine well done (never gamified), interview = prep, applied = steadying.
4. **A3 — rejection care:** light fixed arc, asks if they got feedback (never "paste the email" — ~83% get none),
   distress still escalates via §5. Persona + prompt + 2 new eval personas.
5. **Ride-alongs:** unsave/remove a role (two-step, clears both tables) · direction #1 → equal weight · dead scroll
   hint + its CSS/icon removed.

**Verified:** tsc + lint + production build green; 19/19 unit tests; `eval:advisor` all hard rules held (+2 personas);
3-agent code review → fixes applied. One finding consciously refuted: em dashes in model-facing tool strings are the
established convention (output is sanitised by `stripDashes`), so not changed.

## ▶ LEXI'S LIVE TEST (on staging once pushed)
- Tell the advisor "I got an interview for [a saved role]" → the stage should move AND the Applications pill/nav update
  live, with a warm acknowledgement, WITHOUT being moved to another tab.
- Say "show me my applications" → the advisor should open the Applications panel (user-requested nav).
- Tell it "I didn't get [role], they didn't say why" → acknowledge → normalise no-feedback → turn forward (no "paste
  the email").
- Tell it about an offer → a genuine, specific well done (no gamification).
- Open a saved role → "Remove from applications" (two-step) → it leaves the board + nav.
- Check: Direction view #1 is no longer black-highlighted; the "don't scroll endlessly" hint is gone.

## ▶ NEXT SESSION — recommended focus
Still open from the three themes (see `FEATURE-ROADMAP.md` → "THEME A … SHIPPED" block for the full residual list):
- **Theme B (Applications IA depth):** filter by stage · offer/decline sections · fuller per-application hub.
- **Theme C (nav/polish):** overlay scrollbar · collapsible sidebar · click-a-direction-opens · per-day roles feed.
- Plus the standing opens: account menu popover, archive the dead `/dashboard/*`, a real dated activity timeline.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is never QA) ·
  `/deploy-check` before any merge · receipt ("Logged ✓") the instant Lexi raises anything.
- Stage taxonomy unchanged: saved → preparing → applied → interview → offer → archive.
- Run `npm run eval:advisor` when the advisor PROMPT/persona/voice changes (done this session — prompt gained the
  rejection arc + outcome acknowledgement). Voice rules live in FOUR prompts that drift — keep them in sync.
- New shared module `src/lib/surfaces.ts` (advisor surfaces) — import it, don't re-list the surfaces.
- Pre-launch non-negotiables unchanged (CLAUDE.md): REED_API_KEY, ICO (deferred), privacy/terms + real deletion,
  Sentry + Analytics, solicitor review of outreach, real Resend domain.

## Ready-to-paste prompt for next session (fresh chat)
> Career Intelligence — read START-HERE.md + .session-handoff.md + REBUILD.md, confirm `git branch` shows `* staging`.
> Last session shipped Theme A (advisor drives state live + holds you through outcomes + user-requested navigation).
> This session = pick the next batch from FEATURE-ROADMAP "THEME A … SHIPPED" residuals — likely **Theme B:
> Applications IA depth** (filter by stage, offer/decline sections, fuller per-application hub). Grill once, research-
> ground anything resting on real behaviour, build the batch, one review at the end, give me a receipt every time.
> Stay on Sonnet, normal effort unless it gets architecturally thorny.

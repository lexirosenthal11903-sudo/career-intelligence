# Career Intelligence — Session Handoff (updated 2026-07-02, Session 49)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`).** The API is real money, NOT the Max
   plan. Full `eval:advisor` (~$0.20) ONLY on a material advisor *chat-prompt* change — never twice a session,
   never "to be safe". `npm run eval:advisor:quick` (~$0.03) for spot-checks; lean on the £0 unit/e2e.
3. If the advisor 400s "credit balance too low", it's the balance, not a bug.
4. **The pre-commit e2e gate IS active** — a Claude Code hook in `.claude/settings.json` runs `npm run test:e2e`
   before every `git commit` and blocks on failure (log: `/tmp/meridian-e2e.log`). Don't bypass — re-run to
   classify; if a "flake" repeats it's a real bug (S46). If your change deliberately alters behaviour an e2e
   pins, UPDATE the test to the new contract (that's not bypassing) — S49 did exactly this for the recap.

## ▶ WHAT SESSION 49 DID — Roles-vs-Applications IA BUILT + SHIPPED + live-QA PASS (Opus)
Built `SPEC.md` ("one record, two lenses") end-to-end, plus two live-test fixes. **All on staging, live-QA all
green (Lexi, Claude-in-Chrome, 4/4 tests PASS).** Commits: `2c2d53a` (IA), `ae438b2` (refresh continuity),
`35368d2` (reach-out door). Memory updated: `project_roles_vs_applications_ia.md` + `MEMORY.md`.

**What shipped (detail in the memory file + SPEC.md — don't re-derive):**
- **Prep auto-saves, never auto-advances:** `ensureApplicationSaved` (`src/lib/save-application.ts`) + pure
  `applicationExistsFor` (`role-key.ts`, unit-tested). Wired into `tailor_cv`/`write_cover_letter`/
  `draft_outreach` tools + `/api/tailor-cv`. Creates the app at stage `saved` only if absent (dedupe by
  job_id OR roleKey); NEVER resets an existing stage.
- **Two lenses:** `OutreachSection` moved into `SavedJobDetail` (management); `RoleDetail` gets `ReachOutDoor`
  (pre-save) + quiet "In your applications →" handoff (post-save). No `job_id` added to `outreach` — the
  roleKey join already works (SPEC's "likely add job_id" was avoidable).
- **Reach-out door creates the app ON CLICK** (`saveRoleToApplications`), not via the advisor — live test
  found the advisor short-circuits on pre-existing outreach so no app was created; the door now always saves.
- **Refresh continuity:** recap + "New session" divider gate on `isMeaningfulReturn` (≥6h/new day), not
  recap-exists. Same-session refresh restores the thread inline; genuine return shows the recap. e2e contract
  test added (`state-sync.spec.ts:259`).

**Verify status:** unit 29/29, tsc 0, eslint 0, next build 0, e2e 28/28. Nothing merged to main.

## ▶ KNOWN RESIDUAL (logged, do NOT silently re-discover)
Dual-id duplicate-row edge (`saved_jobs`/`saved_applications`): a prep/door save uses a listing id or a
`chat-<slug>` id, so a narrow race/title-mismatch window can dup a row. Mitigated by the `isInApplications`
roleKey flip; **SPEC lists the dual-table collapse as out of scope.** Full note: `parking-lot.md` (S49 entry).
Live-QA Test D (no duplicates) PASSED, so the common path holds.

## ▶ NEXT (in order — Lexi picks)
1. **Interview prep** — the last major candidate-loop piece. FIRST check `research/interview-prep-research.md`
   against the 7-point research bar (`feedback_research_grounded_building`); if it doesn't meet the bar, do the
   research before building. Then grill/spec before code. Opus.
2. **Process session — session-friction audit → skills/automations/CLAUDE.md fixes.** Lexi runs the audit
   herself first, THEN we implement its output in a fresh session (approvable diffs, Opus NOT Fable — Fable's
   free window ended, 2× Opus). Prereq: sub-agents must be able to READ her transcripts. Brief: `parking-lot.md`.
   Sibling: the "hunt for bugs" prompt is already `/code-review ultra` — run it before a real merge to main.
3. **#6 expired listings** — last HIGH audit item; needs new data + a UX decision. Nearer launch.
4. Smaller logged: advisor tool-selection smell (advisor cited an old outreach on "Help me reach out" — the
   door fix works around it, but the advisor prompt could be sharper); account menu; archive dead `/dashboard/*`.

## Model guidance for next session
- **Interview-prep build → Opus** (Sonnet for mechanical parts). Process/audit implement → Opus. NOT Fable.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is never
  QA) · `/deploy-check` before any merge · design and build sessions are separate.
- Pure helpers needing unit tests go in dependency-clean leaf modules (no `@/`): `role-key.ts`, `outreach.ts`,
  `chat-reply.ts`, `sanitize.ts`, etc. `node --test` imports the `.ts` directly and can't resolve `@/`.
- Voice rules live in several prompts (chat, recap, tailor_cv, cover-letter) — keep in sync; output sanitised
  by `stripDashes`. Em dashes banned in user-facing copy AND context-helper strings.

## Suggested skills for next session
- `/grill-me` then `/brainstorming` before any interview-prep build (grill-first is a standing rule).
- `/deploy-check` if/when Lexi asks to merge staging → main.
- `/code-review ultra` before a real merge to main (the proper "hunt for bugs" tool).
- `/handoff` (or update this file) at the end.

## ▶ READY-TO-PASTE NEXT-SESSION PROMPT
> Career Intelligence — read `.session-handoff.md` + `START-HERE.md`; confirm `git branch` shows `* staging`.
> Session 49 shipped + live-QA-passed the Roles-vs-Applications IA (one record, two lenses), refresh
> continuity, and the reach-out-door app-creation fix (all on staging, nothing merged). Today: **interview
> prep** — FIRST check `research/interview-prep-research.md` against the 7-point research bar; if it falls
> short, do the research before building, then grill + spec before any code. **Opus** (`/model
> claude-opus-4-8`). (Alternative: the parked process session — implement the friction-audit output as
> approvable diffs, Opus not Fable.)

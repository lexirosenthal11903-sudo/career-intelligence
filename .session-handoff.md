# Meridian — Session 41 Handoff (Step 0 done → start the spine, Step 1)

**Date written:** 2026-06-21 (end of Session 40)
**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything)
**Model:** **Opus** for Step 1 (advisor memory/agency + jobs are reasoning-heavy). Sonnet for mechanical edits.

---

## Read first
- **`REBUILD.md`** — single source of truth. Step 0 checklist is now ticked; Part 2 Step 1 is next.
- Session 40 commits: `a0744ee` (the floor), `5996080` (prod rebuild trigger), plus doc/config cleanup.

## ✅ Step 0 — the engineering floor is DONE (built + verified)
1. **Stable URL fixed.** Production branch = `staging`. The one permanent URL is
   **`career-intelligence-xi.vercel.app`** — every push to `staging` updates it, login persists.
   Live gate green (8/8). The old `-staging` alias is dead; `test:e2e:live` now defaults to the new URL.
   - Root cause of the first failed build: `NEXT_PUBLIC_SUPABASE_URL` was only in the Preview env,
     not Production. Added it to Production via CLI; rebuild succeeded.
2. **Migrations + RLS.** All 5 tables now have version-controlled migrations in `supabase-migrations/`
   (`profiles`, `results`, `saved_jobs` added this session) + `README.md` + `verify_rls.sql`.
   Corrected a doc error: `profiles` IS keyed by `user_id` (the prior handoff was wrong).
3. **GDPR.** `delete-account` now erases the `profiles` table too (was missed).
4. **Observability.** Sentry + Vercel Analytics wired; dormant until a DSN is set, so the build stays
   green. Error boundaries forward to Sentry.
5. **Server-first state.** `src/lib/analysisResult.ts` makes `/api/results` the source of truth;
   sessionStorage is now only the pre-auth onboarding fallback. All 7 read sites converted. This kills
   the stale-cache-trust bug class behind the S39 crash. Verified: build clean, smoke 15/15,
   prod regression 20/20 (incl. malformed-data + returning-user hydration), live 8/8.
6. **Enforcement hooks.** `.claude/settings.json` (now tracked): SessionStart branch check +
   pre-commit e2e gate. ⚠️ Hooks go live next session or after opening `/hooks` once (the file didn't
   exist when S40 started, so the watcher hadn't loaded it).

## ✅ Lexi's Step 0 actions — DONE
1. **Migrations + RLS run in Supabase.** All 5 tables verified `rls_enabled = true`. Running them is
   what surfaced the `profiles` schema mismatch (see priority item below).
2. **Sentry active.** `NEXT_PUBLIC_SENTRY_DSN` added to Vercel (Production + Development; Preview skipped
   — CLI quirk, non-critical, add in dashboard if ever needed). Free plan = 5,000 errors/mo, no card.
   Error monitoring only (no replay/tracing) to stay in the free tier.

## ▶ Session 41 — start Step 1 (the spine)
Per REBUILD Part 2. Recommended order (each is its own focused thread, test-gated):
1. **Advisor memory** — evolving model of the user, not a one-shot snapshot pasted into the prompt.
2. **Advisor agency (tool_use)** — it can actually update a direction, save a job, change a stage,
   update the profile. "Done" means something changed.
3. **Jobs that are right** — fix keyword + scoring so seniority/industry are correct.
4. **Kill the childish smiley face**; begin the Meridian rename.
**Done when:** 3–5 real people go through it and the "click" lands for someone who isn't Lexi.

## ⚠️ Step 1 PRIORITY — `profiles` table is broken (found S40 when running migrations)
The live `profiles` table has only **`id` (= auth uid), `email`, `created_at`** — **no `user_id`,
no `data` column.** But `/api/profile` (GET/PATCH) and parts of `/api/chat` query `user_id` + `data`,
so **the profile feature has been silently failing in production.** RLS is now correctly keyed on `id`
(S40). Step 1 must reconcile: either (a) add `data jsonb` to `profiles` + switch the code to key on
`id`, or (b) migrate the table to a `user_id`/`data` shape. `delete-account` was fixed to erase
`profiles` by `id`. This is the kind of doc-vs-DB drift the rebuild exists to kill — the live DB is the
source of truth, not the code comments.

## Step 1 investigation (non-blocking, carried from S40)
- **Why did `suggestedDirections` ever serialise as a string?** Trace `api/analyse` — the pipeline that
  wrote the malformed `results` rows. The server-first refactor + `Array.isArray` guards mean it can no
  longer crash the UI, but the bad data should be understood and old malformed `results` rows cleaned up.
- CV-update path in Profile still does a full re-onboard (`router.push("/input")`) — Step 1 UX fix.

## Lexi's outstanding non-build actions (unchanged)
GitHub token rotation · ICO registration · privacy policy + terms · (REED_API_KEY + Upstash already in
Vercel envs). Migrations + Sentry DSN above.

## Standing rules
`git branch` = `* staging` before touching anything · agree task list upfront, build autonomously, push
once, Lexi tests at end · never touch `main` without explicit instruction · `/deploy-check` before any
merge · proactively state the model to use.

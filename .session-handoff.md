# Career Intelligence — Session Handoff (updated 2026-07-06, Session 52)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`).** The API is real money, NOT the
   Max plan. £0 unit/e2e tests + repro harnesses first; full `eval:advisor` only on a material advisor-prompt change.
3. **Don't switch models mid-session (breaks the cache). The prompt cache is ~5 min TTL now (corrected S52) —
   a short idle goes cold, so `/session-handoff` → `/clear` → new session when stepping away.**
4. State where we are and today's focus, then challenge-before-building.

## Where we are (staging, all pushed to staging — nothing merged to main)

**Session 52 SHIPPED + committed (16a7f5d) — the Anthropic "how we work" research + upgrades:**
- Confirming audit: our ~11 practices strongly align with Anthropic's own guidance. Full cited report:
  `research/anthropic-working-practices-research.md`.
- **CLAUDE.md rule 8** — cache-TTL corrected to ~5 min default (was wrongly ">1h"); batch CLAUDE.md edits.
- **WORKING-PRACTICES** — "2 corrections → /clear" trigger; named default loop (abstract problem → autonomous
  build → review); fenced LLM-Council row in the trigger table.
- **push-to-main guard hook** in `.claude/settings.json` (denies any push to main; tested).
- **LLM Council INSTALLED (fenced)** — `.claude/skills/llm-council/` (local-only; skills are gitignored),
  manual-invoke only (`council this`). Use ONLY for big STRATEGY forks — never routine work. `/grill-me` first.
- **Canary Trick REJECTED** (unsupported mechanism, redundant). Memory: `project_anthropic_working_practices_audit`.

_Not pushed to main. Not run past a live browser test (docs/config only — no live test warranted)._

## ⏭ NEXT SESSION — the FABLE grounding-spec session (FRESH session, do NOT switch models mid-session)

This is the 2nd of the two queued sessions (Anthropic research was 1st, now done). Steps:
1. **Verify Fable isn't already paid on Lexi's plan first** (memory `project_fable_priority`; the free window
   per our notes closed 2026-06-22, so genuinely check).
2. **Cheap top-up:** finish the interrupted verification of the 🟡 tier in `research/job-matching-quality-research.md`
   (targeted, not a full new deep-research pass) before Fable builds on any specific figure.
3. **Then Fable** reasons over BOTH new research docs (`company-factual-grounding-research.md` +
   `job-matching-quality-research.md`) + `application-effectiveness.md` + `interview-prep-research.md` +
   `GROUNDED-KNOWLEDGE-PLAN.md` → writes the grounding SPEC → build on Opus/Sonnet.
4. **ONE deep-research pass per session max** if any new research is needed (memory `feedback_deep_research_one_per_session`).

## Also queued
- **Blind Claude design proposal** (parking-lot, a design session).
- **First real use of the LLM Council** would fit the naming/identity/branding session (a genuine strategy fork).

## Still open from S50/S51 (fold into the next relevant test)
- The interview-prep MENTOR rebuild has NOT been retested live by Lexi. Add to the next browser test: for a
  named employer, is the prep genuinely specific + factual to THAT employer, or generic/made-up?

## Model guidance
This session was Opus (research synthesis) — correct. The Fable session is its own fresh session on Fable
(verify free-vs-paid first). Everyday build/design/copy stays on Sonnet, normal effort.

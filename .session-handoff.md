# Career Intelligence — Session Handoff (updated 2026-06-27 PM #3)

## 🔔 REMINDER FOR LEXI — the old live-test reminder is now RESOLVED
The previous handoff asked you to live-test Outreach Slice 2 + the Arlo rename. **You did that this session.**
Result: the **rename PASSES** (no character name visible anywhere). **Slice 2 has bugs** (see below) — the
biggest is that its entry points were built on a dead surface and are invisible to real users. So there is no
outstanding live-test to carry forward; the next job is the **bug-fix pass**.

## Latest session (2026-06-27 PM #3) — what got done
1. **Interview-prep grounding research DONE — passes the 8-point bar (committed `fb4f7aa`, pushed).**
   `research/interview-prep-research.md`: planning (9 formats, question types by format + role/sector matrix,
   prep techniques) + the live mock (evidence, calibration, feedback, anxiety) + edge cases (ALL-ages) +
   UK-2026 context (Success Profiles, AI-in-interviews, 140:1) + build implications + known gaps + review
   date 2026-12-27. **Independent reviewer agent audited it** (PASS-WITH-FIXES); all 4 must-fixes + 5
   should-fixes folded in (two flagged citations verified real — Wilkie & Rosendale 2024, Brooks 2014 — or
   re-sourced; unverifiable "3,000+/women/2026" async claim removed; 140:1 given its ISE-member caveat).
   **Research passed → interview-prep build is now UNBLOCKED.**
2. **Company-specific intelligence logged** (FEATURE-ROADMAP, cross-cutting). Glassdoor ruled out (API
   dead/enterprise-only, breaks £0 + never-scrape); use published frameworks + the user-paste pattern instead.
3. **Agent & automation timeline logged** (WORKING-PRACTICES, committed + pushed). Full set sequenced by
   trigger; build NEW ones only when the trigger fires; Claude raises each. Key rule: scheduled agents
   check-and-flag, never silently act. **OpenClaw evaluated + rejected** (autonomous local agent via messaging
   apps — security profile disqualifies it near user CVs; Claude Code + `/schedule` already cover our needs).
4. **Memory:** `feedback_complete_ideas_upfront` written — give the FULL set of ideas on the first pass; Lexi
   should never have to ask "anything else?".
5. **Live-tested Outreach Slice 2 + rename** — feedback captured in full to `parking-lot.md` (section
   "Session 2026-06-27 PM — Outreach Slice 2 live-test feedback").

## ▶ NEXT SESSION — the focus (fresh session, Opus, START IN PLAN MODE)
**Outreach Slice 2 bug-fix pass first, then the interview-prep build.** Do the bug pass as ONE batch (don't
ping-pong), push once, Lexi retests the whole outreach flow. Full detail + root causes in `parking-lot.md`.

**🔴 Bug pass (batch — all in `parking-lot.md` PM section):**
1. **Highest priority — entry points on the wrong surface.** The 3 Slice 2 entry points live in
   `/dashboard/roles/RolesPage.tsx` + `[id]/RoleDetailPage.tsx`, but the live surface is `/workspace` →
   `WorkspaceShell` (`SidePanel.tsx` for Direction/Roles, `ChatPane.tsx` for chat). Re-implement the 3 entry
   points in `SidePanel.tsx`. Slice 2 is non-functional in prod until this is fixed. Add a test/INSIGHTS note
   so "shipped to a dead surface" can't pass green again.
2. LinkedIn search link useless in the found-person case (`advisor-tools.ts:607` — searchKeywords = role+company
   only; empty when a person is pasted). Build search from the pasted person, or reframe + soften the link.
3. Chat: no auto-scroll to newest message on send (`ChatPane.tsx:522` endRef not firing on user send).
4. Chat: textarea doesn't reset height after send (stays expanded; `ChatPane.tsx:771`).
5. "+" file-upload button broken when logged in (`ChatPane.tsx:961`).
6. Direction panel: a "1" still renders amber/orange — breaks the locked colour rule; re-token neutral.
7. "Don't scroll endlessly…" hint (`SidePanel.tsx:275`) looks clickable but isn't — make non-interactive or
   focus the chat input.
8. Honesty refinement: tighten the `foundContext` prompt so it never invents "your piece on X" when the bio
   only said they *write about* X.

**🟡 Features captured (DON'T quick-patch — fold into the interview-prep-era "advisor memory + follow-up" build):**
- A. Paste a LinkedIn URL instead of the whole bio. Honest constraint: can't fetch/scrape LinkedIn — paste-TEXT
  stays the route for LinkedIn; paste-LINK works only for other fetchable public pages (existing paste-a-link item).
- B. Outreach follow-up loop (confirm sent → ~1 week later ask if replied / want to follow up, unless already
  heard back). The outreach instance of the planned "advisor remembers + follows up" system.
- C. Advisor must remember "I have no warm network" / "stop asking me that" and skip the hard-coded warm-first
  probe (`advisor-prompt.ts:81`). Real test of the memory promise — currently it keeps asking.

**Then: the interview-prep build** (research is passed). Architecture-heavy → Opus, plan mode. The
`interview_prep` planning tool + the advisor-run mock (format detection + turn-taking) + wire into Applications.
Grounding + build rules in `research/interview-prep-research.md` §13. Touches the advisor prompt → run
`npm run eval:advisor` AFTER it settles, before any merge.

## Operational / standing
- Email still in Resend **TEST MODE** (`onboarding@resend.dev`) — codes only reach Lexi's own inbox; verify a
  real Resend domain pre-launch.
- Anthropic API credits power production — if the live advisor errors, check Billing first.
- `npm run eval:advisor` — run once before any merge to main, not mid-build.
- Standing rules: `* staging` before touching anything · never touch `main` without explicit instruction ·
  build autonomously, push at milestones · £0 for NEW platforms · `/deploy-check` before any merge · no em
  dashes in user-facing copy OR Claude's own writing to Lexi · advisor speaks as "Career Intelligence", first
  person, no character name · give the FULL set of ideas upfront (memory `feedback_complete_ideas_upfront`).

## Ready-to-paste prompt for next session
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> Today: the Outreach Slice 2 **bug-fix pass** (one batch, push once, then I retest the whole outreach flow).
> Full root-cause detail is in `parking-lot.md` under "Session 2026-06-27 PM — Outreach Slice 2 live-test
> feedback". START IN PLAN MODE: read the `/workspace` shell (`WorkspaceShell.tsx`, `SidePanel.tsx`,
> `ChatPane.tsx`, `advisor-tools.ts`) and lay out exactly what changes, in what order, before touching code.
> Top priority = move the 3 Slice 2 entry points onto the live workspace surface (they're currently on the dead
> `/dashboard/roles` page). Then the smaller chat/design bugs. Hold features A/B/C for the interview-prep-era
> "advisor memory + follow-up" build. After the bug pass: the interview-prep build (research has passed —
> `research/interview-prep-research.md` §13). Opus, plan mode.

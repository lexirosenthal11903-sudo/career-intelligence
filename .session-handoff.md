# Career Intelligence — Session Handoff (updated 2026-07-01, Session 45)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`).** The API is real money, NOT the Max
   plan. Run the FULL `eval:advisor` (~$0.20) ONLY on a material advisor *chat-prompt* change — never twice a
   session, never "to be safe". Use `npm run eval:advisor:quick` (~$0.03, 5 personas) for spot-checks, and lean on
   the £0 unit/e2e tests. The recap prompt is NOT covered by the eval.
3. If the advisor 400s "credit balance too low", it's the balance, not a bug.

## ▶ WHAT SESSION 45 SHIPPED (4 commits, all on staging, all green, live-tested 5/5 PASS)
The advisor-vs-screen **drift cluster from `STATE-SYNC-AUDIT.md` is now closed except #6.** Pushed
`93d0fd6` → `d0ac861`. Each slice: tsc 0 · lint 0 · unit + e2e · `/code-review` (findings fixed) · £0 API.

- **Nav "15 vs 13 live" → single source.** Saved-role state (interested/passed/hidden) + the `liveRoles` filter now
  live ONCE in `usePanelJobs`; nav count + panel both derive from it. Removed SidePanel's duplicate `/api/save-job`
  loader. (`93d0fd6`)
- **#1 badge e2e** — synthetic-id advisor-saved role badges "✓ In Applications" once on the live listing
  (roleKey join). `keywordHash` extracted to `src/lib/job-set.ts` for a deterministic seed. (`93d0fd6`)
- **Board-truth eval persona** + **`eval:advisor --quick`** subset. (`93d0fd6`)
- **Autoscroll** — a user turn anchors the new message's START near the viewport top (clamped so short messages
  still sit above the composer), keyed off a SOURCE signal (`anchorNextRef` in `useArloChat`), not "is the list
  scrollable yet". Pure `anchorScrollTop` (`src/lib/chat-scroll.ts`) + a deterministic Playwright scroll-position
  test (`/api/chat`+`/api/recap` mocked, reduced-motion). (`3b78e53`)
- **#13 `update_profile` data-loss** — values/dealBreakers now MERGE (case-insensitive union, capped) via
  `src/lib/merge-list.ts`, never replace. (`e5342f9`)
- **Recap name backstop (#3)** — recap prompt writes a `{NAME}` token that `applyName` (`src/lib/sanitize.ts`)
  substitutes with the resolved display name, so the card can't emit a model-invented shortening. (`e5342f9`)

Full per-item detail: `STATE-SYNC-AUDIT.md` "Progress log". Live-test result + the new finding:
`FEATURE-ROADMAP.md` "Session 45 live-test findings". 50 unit tests; `tests/state-sync.spec.ts` has 4 e2e.

## ▶ NEXT (in order)
1. **Cold error shown on a SUCCESSFUL tool action** (the S45 live-test find, logged in FEATURE-ROADMAP). The tool
   COMMITS (value saved + "✓ Updated your profile" echo + signal) but the chat reply is the cold fallback
   "Something went wrong on my end, say that again?" — the follow-up text generation (the 2nd model call after the
   tool result) failed/empty while the committed action's echo still came through. Same class as the S44
   "committed action never stranded behind a cold error" rule, on a path it didn't cover. **Reproduce FIRST** (a
   harness driving the real tool loop with a forced post-tool failure) before patching; the fix = when actions
   committed this turn, show a warm "Done, I've noted that" instead of ERROR_MSG. Small, valuable warm-up. (Opus
   not needed.)
2. **#6 expired listings** — the last HIGH audit item. NEEDS NEW DATA (Adzuna age / Reed `expirationDate` liveness)
   + a UX decision (hide vs badge "may have closed") + spends Adzuna/Reed quota (£0 constraint). Bring Lexi the
   design choice FIRST; she isn't launching, so this waits until nearer launch. **Opus.**
3. **B.1/B.3** — the full `assembleUserState` server-side consolidation + snapshot test (bigger refactor; the
   local single-source fixes already cover the user-facing symptoms, so not urgent).
4. Smaller logged: #10 (edit/delete advisor memory notes), account menu, archive dead `/dashboard/*`.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is never
  QA) · `/deploy-check` before any merge · receipt ("Logged ✓") the instant Lexi raises anything.
- Reproduction-first debugging: a small harness driving the real tool loop (mock tool results, loop like
  `/api/chat`) catches sequencing/leak/match bugs cheaply. Don't blind-edit working code against a bug you can't
  reproduce. Node runs `.ts` natively (type-stripping); plain `node --test` can't resolve `@/` aliases, so pure
  helpers that need unit tests go in dependency-clean leaf modules (e.g. `merge-list.ts`, `chat-scroll.ts`,
  `job-set.ts`, `sanitize.ts`), not in files that import `@/...`.
- Voice rules live in SEVERAL prompts (chat, recap, tailor_cv, cover-letter) — keep them in sync; the recap is its
  own surface (`/api/recap`) with its own prompt + deterministic sanitisers (stripDashes, stripTimeOfDay, applyName).
- Run `npm run eval:advisor` ONLY on a material chat-prompt/persona change (recap changes aren't covered).

## ▶ READY-TO-PASTE NEXT-SESSION PROMPT
> Career Intelligence — read `.session-handoff.md`, `START-HERE.md`, and CLAUDE.md rule 2 (cost discipline);
> confirm `git branch` shows `* staging`. Session 45 closed the state-sync drift cluster (all live-tested 5/5
> PASS, pushed). Start with the logged S45 finding: the advisor shows the cold ERROR_MSG on a turn where a tool
> action actually SUCCEEDED (value saved + "✓ Updated your profile" echoed). Reproduce FIRST with a harness driving
> the real tool loop (force the post-tool text generation to fail), confirm the path, then fix so a committed
> action shows a warm acknowledgement instead of the cold error. Stay on Sonnet, normal effort; £0 unit/e2e tests
> first, no paid eval (not a chat-prompt change). Then, if Lexi signals launch is near, bring her the #6
> expired-listings UX + £0 decision before building.

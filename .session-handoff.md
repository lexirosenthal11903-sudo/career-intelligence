# Career Intelligence — Session Handoff (written 2026-06-26, evening)

**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything).
**Live (updates on every push):** career-intelligence-xi.vercel.app
**Orient first:** `START-HERE.md` → `MISSION.md` → `REBUILD.md` → this file.

---

## ▶ What this session did (2026-06-26 evening) — test/eval infra FINISHED + token rotated

The whole test/eval infrastructure track is now **done, pushed, and CI-green.** Three commits + docs:

- **Advisor eval** (`a5da7d9`) — pushed, CI green. (Built the prior session; pushed this one.)
- **Lint to green** (`e33f7fc`) — pushed, CI green. Cleared all **47 → 0** problems:
  - **`<a>`→Next `<Link>`:** converted EVERY internal-route link across the dashboard nav + CTAs (the
    App-Router `no-html-link-for-pages` rule only flags a flaky subset — converting all of them fixes the
    full-reload UX bug and keeps the nav consistent). External links (job applyUrl, skill resources, saved
    certs) correctly stay `<a>`.
  - **15 `set-state-in-effect`:** justified per-line `eslint-disable` on each (localStorage/URL/navigator
    reads, data fetches, subscriptions, timed transitions) — none derivable-in-render. **No blanket disable.**
  - Removed 8 unused vars (incl. the fully-dead Applications "timeline expansion" feature), escaped 2
    entities, captured the dialog ref in `TailorCVModal` cleanup, 1 `immutability` disable on an intended
    event-handler nav.
  - Verified before push: lint green, tsc clean, 11 unit + 15 mock e2e pass, build OK — then real CI confirmed.
- **CI gate** (`bc89652`) — pushed, CI green (gates ran for the first time). `ci.yml` now runs
  **lint → typecheck → unit tests → build → mock e2e** on every push. `npm test` uses `node --test` (Node 24
  strips the `.ts` imports natively — **no vitest**; the old comment was wrong).
- **Docs** (`700575f`, `b01b83a`) — CLAUDE.md pre-launch list updated; roadmap additions (below).

**GitHub token rotated + cleaned up (a pre-launch item, now mostly closed):** pushing workflow files needs a
PAT with **`workflow` scope** — the old token lacked it. Lexi created a new classic PAT (repo+workflow,
expires 2026-09-24), it's in the macOS keychain, and she **revoked both old tokens** (incl. the exposed one).
Verified: git auth works, and **nothing in `.env`, Vercel, or app code uses a GitHub token** — it's purely a
git push credential, so no env anywhere needed changing.

## ▶ Two product ideas captured this session (in `FEATURE-ROADMAP.md`, Step 2)
1. **Company-type targeting — PARKED DECISION.** The analysis already generates `companySuggestions` ("types
   of company that suit you + why") every run, and CSS exists, but the dashboard render was never wired.
   Decided NOT to build speculatively (risk: generic / duplicates directions = anti-persona). Decide WHEN we
   build outreach: finish it if it sharpens targeting, else cut it INCL. from generation. (It's currently
   harmless write-only state in `DashboardHome.tsx`.)
2. **The user's agenda — "what we're working through" (needs a brainstorm first).** For the overwhelmed user
   who offloads many wants at once. (A) advisor holds + triages + sequences a multi-topic agenda (behaviour,
   one-at-a-time, moves to next when done); (B) a visible progress surface (to-cover/in-progress/done/parked).
   On-mission + a non-gamified return mechanic. ⚠️ Must NOT become a generic to-do app — advisor owns the
   agenda. Overlaps recap card + memory + decline-pattern → design as ONE system.
3. **Internship application windows (timing intelligence)** — tell users when schemes open/close so they don't
   miss them. Value is clear; the DATA is the hard part (no clean free source) → ties to Step 3 grounded
   knowledge, £0 constraint.

## ▶ NEXT SESSION — back to Step 2 (the candidate-strength loop)
Test/eval infra is done, so the build path reopens. **The gate is the `▶ RESEARCH FIRST` item in
`FEATURE-ROADMAP.md` Step 2:** the **application-effectiveness research session** must happen before building
"why am I not hearing back?", outreach, or interview prep — it grounds the CV/cover-letter prompts AND
everything built after. Full scope in `parking-lot.md` ("Session 2026-06-25 — application-effectiveness
research"). Alternative if Lexi signals: the **agenda-feature brainstorm** (idea #2 above) via `/grill-me`.

## Where we are
Step 2 (candidate-strength loop) — the heart. Done + persisting: CV tailoring, cover letters, Applications
folder, "why am I not hearing back?" diagnosis, the advisor eval, **and now lint-green + a real CI gate.**

## Standing rules
`* staging` before touching anything · never touch `main` without explicit instruction · build autonomously,
push once, Lexi tests on the live URL at flow milestones · £0 for NEW platforms · `/deploy-check` before any
merge · `/session-handoff` at session end · **Claude owns the timing of every working practice (CLAUDE.md
rule 11)** · pushing workflow files now works (new PAT has `workflow` scope).

## Suggested skills for next session
- **`/grill-me`** — before the research session or the agenda-feature brainstorm: question first, build never-first.
- **`/brainstorming`** — to break the "user's agenda" idea into a spec before any build.

## Ready-to-paste prompt for next session
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> Test/eval infra is DONE (eval + lint-green + CI gate, all pushed CI-green). Back to **Step 2 — the
> candidate-strength loop.** The gate is the **application-effectiveness research session** (`▶ RESEARCH FIRST`
> in FEATURE-ROADMAP.md Step 2; scope in parking-lot.md) — it must happen before building "why am I not
> hearing back?", outreach, or interview prep. Run that research session, OR if I signal, `/grill-me` the
> "user's agenda / what we're working through" feature idea (FEATURE-ROADMAP Step 2) into a spec. Sonnet,
> normal effort (switch to Opus only if we move into architecture).

# Career Intelligence — Session Handoff (updated 2026-07-07, Session 53)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real** (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`). The API is real money, NOT the
   Max plan. £0 unit/e2e tests first; full `eval:advisor` only on a material advisor-prompt change.
3. **Don't switch models mid-session** (breaks the cache; ~5 min TTL, so `/session-handoff` → `/clear` when
   stepping away).
4. State where we are and today's focus, then challenge-before-building.

## Where we are (staging, all pushed — nothing merged to main)

**Session 53 SHIPPED + pushed (e1b6f33) — the Fable grounding-spec session:**
- **Fable billing verified:** included on Max only through **July 7 2026** (50%-of-weekly-limit cap; post-outage
  extension). From July 8: prepaid usage credits at $10/$50 per MTok, double Opus. Memory: `project_fable_priority`.
- **Verification top-up DONE** on `research/job-matching-quality-research.md`: taxonomy/crosswalk cluster,
  PLOS ONE skills-adjacency (76%), and recommender benchmarks all confirmed against primary sources, with
  corrections + nuances recorded inline (⬆️🟢 markers). Still 🟡: OECD §4 figures + rsif.2020.0898 specifics
  (cheap 5-min Sonnet job, any later session; not load-bearing).
- **`SPEC.md` = the grounding-layer spec** (the deliverable). Two subsystems: (A) ESCO/O*NET occupation+skills
  layer (grounded matching, skills gaps, stepping-stones with shared-skills explanations), (B) four-layer
  company-facts cascade (frameworks → employer page → paste-invite → honest sector-generic). Five ordered build
  slices, objective done-criteria, licence/attribution requirements. Build on **Sonnet, normal effort**.
  Old roles-vs-applications spec archived (shipped S49).
- ⚠️ Session-limit lesson (memory updated): Fable + parallel subagents burn plan budget fast; one agent chain
  ran away delegating to itself. In Fable sessions keep fan-out small, subagents on haiku/sonnet.

## ⏭ NEXT SESSION — decide first, then run

**The fork (Lexi decides):** Fable-window session 2 (**unified memory layer** spec) is plan-covered **only if it
runs today, July 7**. Options:
1. **Today on Fable** (fresh session, `/model claude-fable-5`): deep-research gathers if needed → Fable writes the
   memory-layer spec. Watch the 50% weekly cap; small subagent fan-out only.
2. **Later on Opus** (`/model claude-opus-4-8`): perfectly capable for this; costs nothing extra on plan.
Same fork applies to Fable session 3 (job-persistence/daily-roles).

**Otherwise the default next build:** SPEC.md slice 1 (ESCO ingest + grounded skills-for-role) on **Sonnet,
normal effort**. The spec is self-contained; read SPEC.md + `GROUNDED-KNOWLEDGE-PLAN.md` first.

## Suggested skills (next session)
- Building a spec slice → `/writing-plans` (turn SPEC.md slice 1 into an ordered plan), then the engineering
  skills (`systematic-debugging`, `zoom-out` — reinstall from the archive per CLAUDE.md).
- Fable session 2 → `/deep-research` ONLY if the memory-layer research isn't already sufficient (ONE pass max,
  memory `feedback_deep_research_one_per_session`); `/grill-me` before locking the spec.
- Before any merge talk → `/deploy-check` (unchanged rule).

## Still open (fold into the next relevant test)
- Interview-prep MENTOR rebuild not yet live-tested by Lexi (S50). SPEC slice 3 supersedes part of this:
  test named-employer prep AFTER slice 3 lands (it grounds exactly that gap).
- OECD + rsif verification remainder (flagged inline in the research doc).
- LMI for All registration (Lexi, when slice 5 starts).

## Model guidance
S53 was Fable (spec design) — correct use. Build sessions: Sonnet, normal effort. Fable only for the two
remaining window sessions if run today; otherwise Opus for reasoning-heavy specs.

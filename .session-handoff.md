# Career Intelligence — Session Handoff (updated 2026-07-01, Session 47)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`).** The API is real money, NOT the Max
   plan. Run the FULL `eval:advisor` (~$0.20) ONLY on a material advisor *chat-prompt* change — never twice a
   session, never "to be safe". Use `npm run eval:advisor:quick` (~$0.03) for spot-checks; lean on the £0 unit/e2e.
3. If the advisor 400s "credit balance too low", it's the balance, not a bug.

## ▶ WHAT SESSION 47 SHIPPED — outreach tracking (2 commits, staging, live, QA-passed)
The candidate-loop's outreach step was one-time (advisor drafted a message, nothing was saved — the panel even
promised "saved here" and saved nothing). It's now a tracked thread. Grounded in a prior-art pass
(Teal/Huntr/folk/Clay) + `research/outreach-research.md §5`: **per-role** tracking (not a cross-role CRM),
**honest self-reported** status (no inbox), **one gentle follow-up** after a working week, owned by the advisor.

- **`outreach` table** — migration `supabase-migrations/20260701_outreach.sql`. **Lexi ran the SQL in Supabase**
  (confirmed live by the passing QA). One row per (user, role). GDPR posture unchanged: stores the user's own
  message + a by-role search + a role-descriptor `person_type`, never a named third party. Wiped on account delete.
- **`src/lib/outreach.ts`** (pure leaf, unit-tested): status model, `businessDaysBetween` (UTC-deterministic),
  `readyForFollowUp` (5 business days), `formatOutreachForAdvisor`.
- **`src/app/api/outreach/route.ts`**: GET (`jsonNoStore`) + PATCH (self-report status; stamps/clears `sent_at`;
  404 on zero-row match so a stale key can't report false success).
- **`draft_outreach`** (advisor-tools) now upserts the draft, preserving an existing status on re-draft.
- **`set_outreach_status`** new advisor tool (sent/replied/no_reply) with an ambiguity guard (a "Product Manager"
  status change can't silently hit "Senior Product Manager").
- **Advisor context** (chat route + `formatOutreachForAdvisor`) surfaces live outreach + flags the one follow-up due.
- **SidePanel "Reaching out"**: real persisted draft + status chips (reuses board chip styling), View message /
  search link / refine. Copy is now true. Optimistic chip reverts to server truth on any failure.
- Code-reviewed (2 agents): fixed wrong-row match, optimistic-revert, UTC date math, PATCH silent no-op.
- **65 unit tests (was 56), 27 e2e, tsc 0, lint 0.** Commits `a3dd545` (feature) + `ad4dd3b` (roadmap log).

**Live QA (Data Analyst · Sagacity, Lexi 2026-07-01): all 4 steps PASS, no bugs.** Draft persists on reload,
chip persists, chat "they got back to me" flips the chip with no click, View message shows body + follow-up line.

## ▶ NEXT (in order)
1. **Roles-vs-Applications IA — DESIGN SESSION, not a build (Lexi raised it in QA).** Two mental modes are bleeding
   together: "Roles for you" detail should be *"should I go for this?"* (company, role, fit); "Applications" detail
   is *"where's my application at?"* (status, CV, cover letter, outreach tracking, notes). Outreach *tracking* is an
   application concern currently living in the live-role view. **The one fork to decide first:** does drafting
   outreach *promote* the role into Applications (all management concerns in one place, live view becomes pure
   company+fit) — Claude's lean — OR are the two just separate templates with no promotion? Surfaces a real gap:
   the live-role view is thin on actual COMPANY info (only the listing) → ties to Step 3 grounded knowledge.
   **This is the one place to switch to Fable 5** (reasoning-heavy trade-off). Pull references, decide, THEN build.
   Full detail: `FEATURE-ROADMAP.md` "Session 47 — outreach tracking shipped + IA finding".
2. **Next candidate-loop build step: interview prep** (the last major piece of the heart; outreach now done).
3. **#6 expired listings** — last HIGH audit item. Needs new data (Adzuna age / Reed `expirationDate`) + a UX
   decision (hide vs badge). Waits until nearer launch. **Opus.**
4. Smaller logged: #10 (edit/delete advisor memory notes), account menu, archive dead `/dashboard/*`.

## Model guidance for next session
- **IA design session → Fable 5** (double Opus cost, no longer free since 2026-06-22 — worth it for this one).
- **Interview-prep build → Opus** (or Sonnet for the mechanical parts). Routine build stays Opus/Sonnet, not Fable.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is never
  QA) · `/deploy-check` before any merge · design sessions and build sessions are separate.
- Pre-commit hook runs the Playwright e2e suite and blocks on failure (`/tmp/meridian-e2e.log`). Don't bypass —
  re-run to classify; a real failure repeats (S46 proved a "flake" can be a real bug).
- Pure helpers needing unit tests go in dependency-clean leaf modules (no `@/` imports): `outreach.ts`,
  `chat-reply.ts`, `api-response.ts`, `merge-list.ts`, `sanitize.ts`, etc. `node --test` can't resolve `@/`.
- Voice rules live in several prompts (chat, recap, tailor_cv, cover-letter) — keep in sync; output is sanitised
  by `stripDashes`. Em dashes are banned in user-facing copy AND context-helper strings (match `user-state.ts`).

## ▶ READY-TO-PASTE NEXT-SESSION PROMPT
> Career Intelligence — read `.session-handoff.md`, `START-HERE.md`, and CLAUDE.md rule 2 (cost discipline);
> confirm `git branch` shows `* staging`. Session 47 shipped **outreach tracking** (per-role persisted drafts,
> self-report status chips, advisor-owned single follow-up) — live and QA-passed, all green. Next is a **DESIGN
> session, not a build**: the **Roles-vs-Applications IA** — differentiate the live-role detail ("should I go for
> this?" / company + fit) from the application detail ("where's my application at?" / status, CV, outreach
> tracking). Decide the one fork first — does drafting outreach promote a role into Applications, or are they
> separate templates? **Switch to Fable 5** for this one (`/model claude-fable-5`); pull design references before
> deciding; do NOT build until the model is agreed. (Or, if Lexi wants to build instead: interview prep is the
> next candidate-loop step — Opus.)

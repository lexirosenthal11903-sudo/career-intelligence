# Career Intelligence — Session Handoff (updated 2026-06-29, PM2)

## What THIS session shipped (4 commits, pushed to staging, live + QA-verified)

First batch under the new session cadence: **"make the candidate-strength loop walkable end-to-end."**
Grilled once → research sweep → built the whole batch → one review → Lexi live-tested → one fix. Done.

1. **Unify saved-role = Applications** (`43660ba`) — one stage-based board. Full detail in
   `FEATURE-ROADMAP.md` → "✅ TAB / IA CLEANUP — SHIPPED" and memory `project_candidate_loop_surfaces`.
   The model: a saved role IS an application; interested lands at a new first stage **"Saved"**
   (Saved → Preparing → Applied → Interview → Offer), aligned across `/api/applications` +
   `save_job`/`set_application_stage` advisor tools. "Recent" opens INSIDE Applications (preselected).
   Stage pill per row (Saved=ghost, live=filled, Offer=green). "Roles"→"Live roles". Count badges
   now NEUTRAL not amber (was a locked-rule violation). Documents library now shows cover letters too.
   In-batch fixes: Applications rows were rendering unstyled; advisor-saved roles were landing at the
   wrong stage.
2. **Stage propagation fix** (`d344033`) — Lexi's QA caught that advancing a stage in the detail view
   didn't reach the list pill or activity area. Detail now notifies the parent (`onStageChange`); list
   row updates in place; activity shows a present-tense "Now: <stage>" line (NOT a faked dated timeline).
3. **Doc updates** (`9484eaf`, `cc5cd54`) — roadmap marked shipped; Lexi's end-of-session idea dump captured.

**Verified:** tsc + lint + production build green; 19/19 unit tests; `/code-review` pass (found + fixed the
advisor-stage bug); Lexi live-QA'd the flow in Chrome (steps 1,2,3,5 pass; step 4 failed → fixed → confirmed).

## The research that grounded it (don't redo — reuse)
Teal/Huntr/Simplify/LinkedIn prior-art sweep. Verdicts: saved = first stage of ONE pipeline (market
consensus); **list-with-stage-pill beats Kanban at 1–5 applications** (empty columns shame an anxious
user, like an empty streak); per-job doc hub + global library is the dominant doc pattern. Lexi corrected
me mid-session for grilling on opinion BEFORE doing the prior-art work — run the sweep first, then decide.

## ▶ NEXT SESSION — recommended focus
**Theme A: the advisor drives state + holds you through it** (the moat, not polish). From
`FEATURE-ROADMAP.md` → "▶ NEXT-SESSION CANDIDATES" (13 items, 3 themes, all logged this session):
- **A1 — advisor updates Applications from conversation.** ⚠️ `set_application_stage` ALREADY EXISTS —
  VERIFY live first (it may already work for "I got an interview"), then make it reliable + add the
  advisor→navigation bridge ("take you to the right tab"). Generalises to every feature.
- **A2 — advisor acknowledges stage changes** (Offer = genuine "well done", never gamified).
- **A3 — rejection/didn't-get-it care** (read the email, comfort, turn into prep). Emotional-arc differentiator.
- **Cheap ride-alongs** (Theme B/C): **unsave a role** (real gap, DELETE APIs exist — needs UI), the dead
  "don't scroll endlessly" hint, direction #1 black highlight → equal weight (supports honest-matching).

Run it as the cadence: grill once upfront (esp. how far the rejection-care advisor goes — emotional boundary),
research-ground anything resting on real behaviour, build the batch, one review, Lexi live-tests at the end.

## Operational / standing
- `* staging` before touching anything · never `main` without explicit instruction · self-verify (Lexi is
  never the QA) · `/deploy-check` before any merge · give a receipt ("Logged ✓") the instant Lexi raises anything.
- Stage taxonomy is now **saved → preparing → applied → interview → offer → archive** across API + advisor tools.
- Run `npm run eval:advisor` when the advisor PROMPT/persona/voice changes (this session's advisor-tools edit
  was stage-label-only, no voice change → eval skipped deliberately). Voice rules live in FOUR prompts that drift.
- Inert dead code to sweep: the `"saved"` PanelView type member + `TAB.saved` (no longer routed).
- Pre-launch non-negotiables unchanged (CLAUDE.md): REED_API_KEY in Vercel, ICO registration, revoke OLD
  GitHub token, privacy/terms + real deletion, Sentry + Analytics, solicitor review of outreach, real Resend domain.
- Stray `.design/tools/shots/foundation-first.png` still untracked on purpose.

## Suggested skills for next session
- `/grill-me` — once, upfront, esp. on the rejection-care emotional boundary (how far the advisor goes).
- Research sweep (WebSearch) — ground anything resting on real behaviour before building (prior-art rule).
- `/deploy-check` — only if a merge to main comes up (not planned).

## Ready-to-paste prompt for next session (fresh chat)
> Career Intelligence — read START-HERE.md + .session-handoff.md + REBUILD.md, confirm `git branch` shows
> `* staging`. Last session shipped + QA-verified the candidate-loop surfaces (saved = Applications, one
> stage-based board). This session = **Theme A from FEATURE-ROADMAP "NEXT-SESSION CANDIDATES": the advisor
> drives state + holds you through it.** Start by VERIFYING whether `set_application_stage` already updates
> Applications when I tell the advisor "I got an interview" (it may already work), then make it reliable and
> add the advisor→tab navigation bridge; plus advisor acknowledging stage changes (Offer = real "well done")
> and rejection care. Ride along the cheap fixes: unsave a role, the dead "don't scroll endlessly" hint,
> direction #1 equal weighting. Grill me ONCE upfront (esp. the rejection-care emotional boundary), research-
> ground before building, build the batch, one review at the end. Give me a receipt every time I raise
> something. Stay on Sonnet, normal effort unless it gets architecturally thorny.

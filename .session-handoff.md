# Meridian — Session 42 Handoff (Step 1 spine built → verify with real data, then Step 2)

**Date written:** 2026-06-21 (end of Session 41)
**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything)
**Model:** **Opus** for Step 1/2 product work. Sonnet for mechanical edits.

---

## ✅ Step 1 — the spine is BUILT (Session 41)
Pushed to `staging` → live at `career-intelligence-xi.vercel.app`. Five threads, each test-gated:

1. **Profiles fix.** Live `profiles` table was keyed `id` with only `id/email/created_at`; `/api/profile`
   and `/api/chat` wrongly queried `user_id` + `data`, so the profile feature + advisor memory silently
   failed. Now: migration adds `data jsonb` + `updated_at` keyed on `id`; all access via
   `src/lib/profile.ts`. e2e: `tests/profile-roundtrip.prod.spec.ts`.
2. **Advisor memory + agency = one tool_use system.** `src/lib/advisor-tools.ts` (remember,
   update_profile, update_direction, save_job, set_application_stage); server-side tool loop in
   `src/app/api/chat/route.ts`. Memory is the `remember` tool → `profiles.data.memory`, read back by
   `buildUserContext`. Real DB changes, echoed visibly in chat (`meridianActions` → green ✓ lines).
   e2e: `tests/advisor-agency.prod.spec.ts`.
3. **Advisor initiates.** `useArloChat` opens the conversation on first load (no prior history) — Arlo
   speaks first instead of waiting.
4. **Jobs that are right.** `src/app/api/jobs/route.ts`: seniority-aware search (`what_exclude` for
   entry-level) + nationwide default (no more forced London). e2e: `tests/jobs-seniority.prod.spec.ts`.
5. **Smiley killed + Meridian rename begun.** Advisor avatar → abstract meridian mark on every dashboard
   panel + input; wordmark → "Meridian" in nav, dashboard sidebars, page title. Smoke updated to match.

All green locally: `npm run build` clean, dev smoke passes (the pre-commit e2e gate fired and passed).

## ⬜ Lexi's two clicks before this is verifiably live
1. **Re-run `supabase-migrations/20260612_profiles.sql`** in the Supabase SQL Editor — it now adds the
   `data jsonb` + `updated_at` columns (idempotent, `ADD COLUMN IF NOT EXISTS`, no data loss). **Until
   this runs, profile saves + advisor memory fail in production**, and the migration-gated prod tests
   (`profile-roundtrip`, `advisor-agency`) can't pass.
2. After the migration: run `npm run test:e2e:prod` (needs `ANTHROPIC_API_KEY` + Adzuna keys in env,
   already in `.env.local`) to confirm profile round-trip, advisor agency/memory, and jobs seniority.

## ▶ Session 42 — verify the click lands, then start Step 2
- **First:** go through the product as a real user (after the migration). Does Arlo open with something
  specific? Does "I'll set that direction aside" actually change state and show the ✓? Are the jobs the
  right seniority? This is the "does the click land" check the spine exists for.
- Then **Step 2 (the body, advisor-driven):** CV tailoring per job, cover letter per job — both
  memory-aware. See REBUILD Part 2 Step 2 + Part 9.
- **Staged follow-ups (not yet done):** conversation-first UI surface; remaining smiley faces on
  hero/loading/error/404/crash (tied to the deferred homepage redesign); full Arlo→Meridian advisor
  rename (held to pre-university-pitch per S37).

## Standing rules
`git branch` = `* staging` before touching anything · agree task list upfront, build autonomously, push
once, Lexi tests at end · never touch `main` without explicit instruction · `/deploy-check` before any
merge · the pre-commit e2e gate + SessionStart branch check in `.claude/settings.json` are live and
enforced (both fired this session).

# Career Intelligence — Session Handoff (written 2026-06-27)

**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything).
**Live (updates on every push):** career-intelligence-xi.vercel.app
**Orient first:** `START-HERE.md` -> `MISSION.md` -> `REBUILD.md` -> this file.

---

## What this session did (2026-06-27)

1. **Anonymous-auth migration BUILT + pushed** (the sign-up carry-forward fix — `docs/plans/anonymous-auth-migration.md`).
   New `src/lib/anonAuth.ts` (`ensureAnonSession`), wired into `useFirstSession` (saves now authed), `ChatPane`
   gates go-live on `isReal` (anon has a userId but no real account), `AuthModal` converts anon->permanent
   (`updateUser` + `email_change` OTP; Google `linkIdentity`). Old sessionStorage path kept as fallback.
   **Lexi updated the Supabase "Change Email Address" email template to include `{{ .Token }}`** so the in-place
   6-digit code works. Verified: tsc + lint + 11 unit tests + build + 15 e2e mock. **⏳ AWAITING LEXI'S LIVE TEST.**
2. **Established the Research-Grounded Building standard** (`WORKING-PRACTICES.md` + memory `feedback_research_grounded_building`):
   8-point bar, source hierarchy (primary/institutional > commercial), and an independent-review step (writer/reviewer
   split for research). Lexi's completeness pushback added point 8 + the review step.
3. **Outreach research** (`research/outreach-research.md`): 2 passes (24-sector matrix + 5 edge cases, zero-network
   prioritised, communities/events) -> independent audit -> blocker + 9 should-fixes applied -> passed the bar.
   Build-ready for the outreach feature.
4. **Retro-audited the two LIVE grounding docs** and fixed what the audits caught (pushed live, `64f5978`):
   - **Safeguarding (highest stakes):** the distress-signpost behaviour was in `ADVISOR_PERSONA.md` §5 but MISSING
     from the live prompt. Now encoded in `advisor-prompt.ts` (Samaritans 116 123 / GP / counselling). Closes a
     duty-of-care gap (aligns with Ofcom chatbot expectations).
   - **Factual:** 49%->46% (employer-AI, ISE) in prompt + persona + research; corrected the misread "cheating
     7->15%" stat; removed the untraceable 65/95 folklore stat (anchored to Matthews 2014); flagged the
     unverifiable pandemic/2008 comparison + the 52% figure.
5. **Decisions:** product caters for ALL ages/levels (early-career = go-to-market BEACHHEAD, not the ceiling);
   canonical industry taxonomy = National Careers Service + ONS SOC/SIC (Prospects = graduate lens only).
   **Parked (need own conversation):** synthetic-persona "co-worker" QA agents; a communities/events feature.

## ▶ NEXT SESSION — in order
1. **FIRST: residual known-gaps doc pass** (retrofit the two pre-bar docs to the standard). For BOTH
   `application-effectiveness.md` and `mentorship-research.md`: add a formal "Known gaps / lower-confidence areas"
   section; re-tier a few citations as practitioner/commercial + add an institutional source for the mentorship
   "refer on" boundary (EMCC/CIPD); add an all-ages scope note (not graduate-only); add review dates. _(Mechanical
   doc work — Sonnet, normal effort.)_
2. **THEN: build Slice 1 of outreach** — the grounded advisor capability. A `draft_outreach` tool (mirrors
   `tailor_cv` / `write_cover_letter`): warmest-path-first, ask = a 20-min conversation (never a job/CV), one
   follow-up max, sector-calibrated, GDPR-safe (name the right type of person -> LinkedIn deep-link search ->
   co-draft -> user sends). Contextual + advisor-led (Lexi's shape). Then Slice 2 = the role-page entry points.
   _(Advisor-grounding is nuance-sensitive -> consider Opus for that part.)_
3. **Lexi's live test of the anon-auth migration** (whenever she has 5 min): fresh/incognito window -> first
   session + sign up -> the advisor should already know her CV + directions. Bonus: Google sign-up; existing-account sign-in.
4. **Run the advisor eval ONCE** (`npm run eval:advisor`) after the prompt changes settle — the safeguarding +
   46% edits changed the prompt. Pay once, not mid-iteration.

## Model for tomorrow
Start on **Sonnet, normal effort** for the known-gaps doc pass. Switch to **Opus** only for the Slice 1
advisor-grounding (nuance-sensitive). Don't switch mid-thread (breaks cache).

## Standing rules
`* staging` before touching anything · never touch `main` without explicit instruction · build autonomously,
push at milestones · £0 for NEW platforms · `/deploy-check` before any merge · no em dashes anywhere · the
advisor informs + signposts but never narrates its own caveats · **research-grounded building: no grounding,
no build; every grounding report ships with a known-gaps section and faces an independent review.**

## Ready-to-paste prompt for next session
> Career Intelligence — read START-HERE.md then .session-handoff.md, confirm `git branch` shows `* staging`.
> First task: the residual known-gaps pass on `research/application-effectiveness.md` + `research/mentorship-research.md`
> (known-gaps sections, citation re-tiering + an institutional source for the mentorship "refer on" boundary,
> all-ages scope notes, review dates). Then build Slice 1 of the outreach feature, grounded in
> `research/outreach-research.md` (advisor-led `draft_outreach`: warm-first, ask-for-a-conversation, one follow-up,
> sector-calibrated, GDPR-safe deep-link search + co-drafted message). Sonnet/normal for the doc pass; Opus for the
> advisor grounding. I still need to live-test the anon-auth migration. Run `npm run eval:advisor` once after the
> prompt settles.

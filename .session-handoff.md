# Career Intelligence — Session Handoff (updated 2026-07-05, Session 50)

## ▶ AT SESSION START
1. `git branch` → confirm `* staging` before touching anything. Read `START-HERE.md` + this file.
2. **Cost rule is real (CLAUDE.md rule 2 + `feedback_eval_cost_discipline`).** The API is real money, NOT the
   Max plan. £0 unit/e2e tests + repro harnesses first; full `eval:advisor` only on a material advisor-prompt change.
3. State where we are and today's focus (below), then challenge-before-building.

## Where we are (staging, all pushed — nothing merged to main)

**Shipped + self-verified this session:**
- **CV/cover-letter orphaning fixed** at source (canonical-id resolution + a one-time reconcile migration Lexi ran).
- **Interview prep now lives IN the application**, then **rebuilt as a MENTOR, not a vending machine** — the tool
  generates each question + "what they're really asking" (coaching) + a STRUCTURE scaffold (STAR); the answer box
  is the **user's own, empty**; the advisor coaches it out via the mock and NEVER authors it. Verified against the
  real API (advisor fires the tool on turn 1; generation coaches with zero written answers). 87/87 unit tests green.
- **`ackForSilentCommit`** now names the action ("Done. Saved interview prep for X") instead of a generic line.

**⚠️ Lexi has NOT retested the mentoring rebuild yet.** Fold it into the next test (see the added factual check).

## Two big principles locked this session (read them)
- **CLAUDE.md principle 7 — MENTOR, not vending machine** (+ memory `feedback_mentor_not_vending_machine`):
  every candidate-loop feature must teach/guide/draw-out/practise, never hand the user finished substance to edit.
  MANDATORY: cross-check MISSION + the relevant research doc and STATE it before writing code.
- **Factual + specific always** (memory `feedback_ground_facts_free`, reinforced 2026-07-05): as specific to what
  the user is doing, and as factual, as possible — real sources, never made up.

## RESEARCH DONE (Session 51, 2026-07-06) — two of three passes complete; Fable spec is next

The research-gather step is **done** for the two genuinely un-researched threads (the third, CV/cover-letter, was
already covered by `application-effectiveness.md` — re-researching it would have redone verified work, so skipped):
- **(c) Company-factual grounding → `research/company-factual-grounding-research.md`** (COMPLETE, 22/25 claims
  verified 3-vote). Headline: per-employer data exists free for only ~0.15% of UK employers, so the honest
  fallback IS the main path. Do NOT scrape Glassdoor/Indeed (legally hazardous, two grounds). Clean 4-layer £0
  architecture: official frameworks → fetch employer's own page → user-paste-the-invite → sector-generic fallback.
- **(a) Job-role matching quality → `research/job-matching-quality-research.md`** (SEARCH complete, VERIFICATION
  cut short by the session limit — 5 claims 3-0, ~20 gathered-but-unverified from primary sources, tiered 🟡).
  Headline: ESCO + O*NET are free/clean grounding; adjacency via skill-overlap (~76% predictive); OECD evidence
  validates discovery-first + honest breadth-before-narrowing.

**⚠️ Cost lesson (memory `feedback_deep_research_one_per_session`):** deep-research = ~105 agents / 2–4M tokens per
pass. Two back-to-back tripped the account session limit. **Run ONE per session.**

### NEXT SESSION — the Fable spec step
1. **First, a cheap top-up:** finish the interrupted verification of the 🟡 tier in the matching doc (targeted, not
   a full new pass) before Fable builds on any specific figure.
2. **Then Fable** reasons over BOTH research docs (+ `application-effectiveness.md`, `interview-prep-research.md`,
   `GROUNDED-KNOWLEDGE-PLAN.md`) and writes the grounding spec → build on Sonnet/Opus.
3. **Model:** verify Fable isn't already paid first (memory `project_fable_priority`; the free window per our notes
   closed 2026-06-22, so this genuinely needs checking). Don't switch models mid-session.

**Add to the next test (pass/fail):** for a named employer, is the interview prep genuinely specific + factual to
THAT employer, or generic/made-up?

## ⏭ TWO SESSIONS QUEUED NEXT — order set by Lexi 2026-07-06: ANTHROPIC RESEARCH FIRST, then FABLE. Both FRESH sessions.

1. **"How we work" upgrade / RESEARCH session (do FIRST — Lexi's call).** Focus = **Anthropic's own published
   Claude Code / working-with-Claude guidance** as the spine. **Oli (buildwitholi) scan: DROPPED for now** (Lexi
   2026-07-06 "maybe leave out Oli" — add back only if she asks). Does NOT need Fable — use **Opus + deep-research**.
   ⏰ At session start, ask Lexi to share the **LLM Council** + **name-trick** articles. **ONE deep-research pass
   per session** (memory `feedback_deep_research_one_per_session`).
2. **FABLE spec session (second).** Fable is confirmed still available but time-limited — spend it only on what
   needs it. Fable priority #1: (a) cheap verification top-up on the matching doc's 🟡 tier, then (b) Fable reasons
   over BOTH new research docs + `application-effectiveness.md` + `interview-prep-research.md` +
   `GROUNDED-KNOWLEDGE-PLAN.md` → writes the grounding SPEC → build on Opus/Sonnet. Check free-vs-paid on her plan
   at the start; start fresh (don't switch models mid-session).

_Note: order flipped from the earlier Fable-first recommendation at Lexi's request. Keep them as separate fresh
sessions either way (deep-research is one-pass-per-session; switching models mid-session breaks the cache)._

## Also queued
- **Blind Claude design proposal** (parking-lot, a design session).

## Model guidance
Start the research session on **Opus** (research synthesis). The DESIGN reasoning after the facts are in is the
**Fable** step — its own session (don't switch models mid-session; verify Fable isn't already paid first).

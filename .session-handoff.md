# Meridian — Session 40 Handoff (finish the floor + fix deployment)

**Date written:** 2026-06-21 (end of Session 39)
**Branch:** `staging` (confirm `git branch` shows `* staging` before touching anything)
**Model:** **Opus.** Remaining floor work is data-layer + config — reasoning-sensitive. Sonnet only for mechanical stretches.

---

## Read first
- **`REBUILD.md`** — single source of truth (audit Part 1, sequence Part 2, strategy Part 7, etc.).
- Session 39 commits carry the full diagnosis: `fb2b876` (boundaries + test gates), `160448b` (crash fix).

## Session 39 result — the crash is FIXED ✓
"This page couldn't load" for logged-in users was a **data-shape crash**: stored analyses with
`profile.suggestedDirections` saved as a **string** (older schema) → `.map`/`.find` threw on
Roles/Profile/Applications/Role-detail. Home/Skills survived via `Array.isArray`. Profile also cached
the bad data into sessionStorage, cascading the crash to Applications.
- **Fix:** guarded every read site with `Array.isArray` (5 files). Verified on a live deploy logged in
  with Lexi's real data — all tabs render, no error boundary, no `.map` errors.
- **Error boundaries** added (Arlo's voice) so any future crash degrades gracefully, never a dead page.
- **Test gates** added (all green): `test:e2e` (dev smoke), `test:e2e:prod` (hydration + auth + error
  boundary + malformed-data regression, against a real build), `test:e2e:live` (hits the real URL,
  asserts 200 — catches stale deploys).

## ⚠️ THE BIG FINDING — deployment is misconfigured (this is why we went in circles)
- **No Production deployment exists** — every Vercel deploy is a "Preview".
- `career-intelligence-staging.vercel.app` alias is **pinned to a ~9-day-old build** and does NOT follow
  pushes. That old build 404s on /dashboard/roles|applications|profile|skills. Fixes never reached it.
- The fix is live on the latest per-deploy URL; the stable alias is not.
- **Lexi action (agent was blocked from doing it):** re-point the alias OR make `staging` the Production
  branch in Vercel so the stable URL always tracks latest. First confirm which URL Lexi opens to test:
  `vercel alias set <latest-deploy-url> career-intelligence-staging.vercel.app`

## Step 0 floor — remaining, in order
1. **Fix deployment/alias config** (above). Highest leverage. Then `npm run test:e2e:live` should pass.
2. **Schema → migrations + verify RLS** on all 5 tables. NOTE: `profiles` is keyed by `id`, has **no
   `user_id` column** — `/api/delete-account` GDPR erasure must be checked against this (REBUILD #7).
3. **Sentry + Vercel Analytics.** Boundaries have `TODO(Step 0 — observability)` hooks ready.
4. **Move app state off `sessionStorage`** onto the server as source of truth.
5. **Enforcement hooks** (settings.json) via `update-config` — branch check + tests-before-done.

## Other open items (verified, not blocking the crash)
- Investigate WHY `suggestedDirections` serialised as a string (api/analyse pipeline). Consider a one-off
  cleanup of malformed `results` rows. Step 1 work.
- CV-update path in Profile = full re-onboard (`router.push("/input")`) — wrong behaviour, Step 1 fix.
- `e2e-test@meridian.test` Supabase user holds a copy of Lexi's seeded data (for reproduction). Harmless.

## Lexi's outstanding non-build actions
- Deployment/alias config (above) · GitHub token rotation · ICO registration · privacy policy + terms ·
  REED_API_KEY + Upstash env vars in Vercel (Production once it exists).

## Standing rules
`git branch` = `* staging` before touching anything · agree task list upfront, build autonomously, push
once, Lexi tests at end · never touch `main` without explicit instruction · `/deploy-check` before any
merge · proactively state the model to use.
